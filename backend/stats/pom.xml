<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.guide</groupId>
        <artifactId>guide-backend</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>stats</artifactId>
    <name>stats</name>
    <description>统计看板：准确率/分布/盲区</description>

    <dependencies>
        <dependency>
            <groupId>com.guide</groupId>
            <artifactId>common</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        </dependency>
        <!-- 聚合来源：guide_record / chat_message（chat）+ cluster_bucket / root_cause（feedback） -->
        <dependency>
            <groupId>com.guide</groupId>
            <artifactId>chat</artifactId>
        </dependency>
        <dependency>
            <groupId>com.guide</groupId>
            <artifactId>feedback</artifactId>
        </dependency>
        <!-- 只读：dept 名称回填（记录列表要显示推荐/实际科室名，且停用科室的历史记录也要能显示） -->
        <dependency>
            <groupId>com.guide</groupId>
            <artifactId>kb</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
