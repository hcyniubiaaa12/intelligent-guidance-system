# 开发进度 —— 施工前必读，完成后必更

> 规则（写进 CLAUDE.md，双向约束）：
> 1. **施工前**：先读本文件，了解各链路现状、未接通的假数据点、踩过的坑，再读 `CONTEXT.md` 对齐术语
> 2. **完成后**：立即回写本文件——完成内容、耗时、遇到困难点与解法、新产生的待办
>
> 链路代号见《总体架构与链路设计.md》：A 患者导诊对话 / B 知识库入库 / C 反馈闭环 / D 鉴权。

## 总览

| 链路/模块 | 进度 | 状态说明 |
|-----------|------|---------|
| 公共件 common | 100% | 统一返回体/错误码/全局异常/基础实体/Redis/MinIO/**pgvector/ES** 工具均就绪 |
| D 鉴权（后端） | 100% | Security+JWT+Redis 登录态、封禁校验、单测齐全 |
| D 鉴权（前端） | 100% | 登录/注册/登出接真实接口；等待真实环境联调 |
| D 用户管理（后端） | 100% | 账号封禁/解封 + 敏感词库 CRUD + 批量导入 + **违规处置（警告/禁言/解禁）**，单测 31 个全过 |
| D 用户管理（前端） | 100% | UserManage 两 tab 接真实接口（搜索/封禁/词库维护/导出/**处置规则配置 + 触发次数与明细**）；待联调 |
| D 鉴权（联调） | 100% | 2026-09-19 真实环境联调通过：登录/封禁/解封/敏感词全链路 + Edge UI 实测零 console 错误 |
| **敏感词累计处置** | **100%** | 滑动窗口按用户累计命中词次 → 警告/禁言（观察词不禁言）+ 管理端展示与配置；**待真实环境联调（需先执行迁移 SQL）** |
| 前端骨架 | 100% | 路由/守卫/双端样式方案/响应式适配就绪 |
| **LLM 适配层** | **100%** | DeepSeek 流式对话 / 阿里 embedding / 阿里 rerank 三客户端 + 单测 6 个；Key 缺省即报 7000 |
| **RAG 检索层** | **100%** | 改写→双路召回(pgvector+ES)→RRF→重排→Prompt→解析；单测 14 个 |
| **A 对话（后端）** | **100%** | 会话状态机/SSE 七事件（session·delta·question·result·notice·done·error）/追问双路径/敏感词前置/规则门槛（无效输入全轮次 + 追问预算护栏）/结论落库，真实模型联调通过 |
| **A 对话（前端）** | **100%** | Chat.vue / SimRegister.vue 接真实 SSE 与接口，假数据全部移除；首页＝左栏会话目录 + 右对话舞台（点会话只读回放 + 问题书签），空状态「接诊单」与输入区一起居中，输入区为整块书写卡片；**DS 式布局：侧栏贴窗口最左、自带标识、底色与对话区拉开、可收成只剩标识的精简轨、对话区顶栏整条移除、消息一键复制**（0920 三版） |
| **就诊记录（患者端）** | **100%** | 已并入**首页左侧栏**：按天折叠的会话目录 + 只读回放 + 问题书签（自上而下 = 问题 1→最新，放不下时折起来可滚），结论卡从快照重建；独立页 `/records` 已删、老链接重定向首页；后端单测 8 个 |
| **B 知识库（上传流水线）** | **95%** | **后端全通 + 管理端页面接通 + 真语料实测**：格式分流（DocumentMind / 原生读 / Tika）→ 切分三层 → 两段式异步编排 → 三写入库 → 上传/进度/重处理/删除 + 科室/台账/术语维护接口；**18 份测试语料（PDF/PNG/MD）全量入库通过**，检索链路实测文档级召回 88.2%、Top1 命中 79.4%（详见施工日志）。**未接**：术语抽取（旁路，按设计可先不做；白名单的人工新增等它） |
| **C 反馈闭环（前半）** | **30%** | 三触点埋点 + 挂号确认写 actual_dept/命中标记 + filter_log 旁路落库；聚合归桶/审核回流未动工 |
| **统计看板** | **60%** | 准确率/趋势/根因分布/记录列表接真实聚合（stats 纯读侧）；离线侧定时聚合未动工 |
| 管理端页面 | 60% | 见下表（设计口径见《总体架构与链路设计.md》§7） |

管理端五页逐页状态：

| 页面 | 状态 | 依赖 |
|------|------|------|
| 用户管理 | 已接通 | 链路 D（用户分页/封禁 + 敏感词库 CRUD + 违规处置的配置与展示） |
| LLM 配置 | 已接通 | `sys_config` 白名单 + llm 适配层探测 |
| 数据看板 | 已接通 | 链路 A/C 的数据 + stats 纯读聚合 |
| 审核队列 | 未接通（**假数据已清**，现为诚实空态） | **链路 C 后半**：聚合归桶未动工，`cluster_bucket` / `review_task` 现为空表。页面缺的是**后端**，不是接线 |
| 知识库管理 | **已接通**（2026-09-26） | 四个 tab 全接真实接口：科室蓝本（增改启停）/ 文档入库（上传·进度·切片·重处理·删除）/ 映射台账（只读）/ 术语白名单（启停） |

## 施工日志

### 2026-09-26 · 清掉管理端剩下的假数据（审核队列页 + 两处假控件）

**范围**（用户定案「先只清假数据，暂不动工」）：审核队列页的三块后端仍是"从缺"，本轮**不动工**，只把页面上的假东西清掉——留着假数据比空着更糟：**页面上的数字会直接被当成待办数去处理**。

清掉的三处：① `Review.vue` 整页假数据（科室池、聚合桶、回流预览模板、一份本地镜像的根因字典）② 侧栏「审核队列」上**写死的珊瑚色待办徽标 `badge: 3`** ③ 顶栏那个**没有 `@click` 的「近 7 天」死按钮**——它挂在**每一个**管理端页面上，点下去毫无反应。

- 审核页现在是一个说清原因的空态（不渲染空表头，前端设计方案 §3.5/§3.8）：列出待接的三块（聚合归桶 / 读与审 / approve 产出）与图纸位置。重新开工时记得**根因字典从后端取**，别在页面里再抄一份（见 数据库设计 §0）
- **顶栏区间按钮做成了真的**（不是删掉）：只在数据看板渲染，近 7 天 / 近 30 天可切，状态放**路由 query**（`?days=30`）——刷新不丢、链接可分享；`Dashboard.vue` 的 `days` 从写死常量改成跟随 query 的 computed + watch。看板的页面副标题也从「近 7 天 · 口径…」改成「口径：已反馈导诊记录（区间由顶栏切换）」，避免与切换后的区间打架

**验证**（`frontend/e2e_review_clean.cjs`）：审核页表格数 0、旧假数据痕迹（代表样本名、方向名）全为 false、侧栏徽标 0 个；看板顶栏两个按钮、切到 30 天后 URL 变 `?days=30` 且 KPI 按新区间重取（「样本 14 条 · 上期无样本，不可比」——真实聚合）；用户管理页顶栏按钮 0 个；**console/page 错误 0 条**。

**至此管理端不再有任何假数据**：五个页面里四个接通真实接口（看板 / 知识库 / 用户 / LLM 配置），审核队列是**诚实的空态**——它缺的是后端，不是页面。

### 2026-09-26 · 修：管理员第一次登录被送进患者端（用户报的）

**现象**：全新访客打开站点（根路径 `/`）→ 用管理员账号登录 → 落在**患者端首页**，看着像进错了口。

**根因**：`Login.vue` 的落点是 `router.push(route.query.redirect || (role === 'admin' ? '/admin/dashboard' : '/'))`——**`redirect` 的优先级压过了角色**。而访客打开根路径时，路由守卫必然把他送到 `/login?redirect=/`（`/` 就是患者首页）。于是"从根路径进来的管理员"被这个 redirect 送进患者端。
**只有第一次会碰到**：之后浏览器已有登录态，直接进 `/admin/**` 不再经过登录页——这正是"第一次登录"这个说法的来源。

**修法**：落点**由角色决定，redirect 只在它指向该角色有意义的页面时才优先**（`landingPath(role)`）：管理员只认 `/admin` 开头的 redirect，患者只认非 `/admin` 的。反方向同理——患者带着 `redirect=/admin/kb` 来时不再"先跳过去再被守卫弹回来"。

**回归**（`frontend/e2e_login_route.cjs`，五个场景全过）：

| 入口 | 账号 | 修复前 | 修复后 |
|---|---|---|---|
| `/`（首次访问） | admin | **`/` 患者端 ❌** | `/admin/dashboard` ✅ |
| `/login` | admin | `/admin/dashboard` ✅ | 同 ✅ |
| `/admin/users` | admin | `/admin/users` ✅ | 同 ✅（深链仍然认） |
| `/` | 患者 | `/` ✅ | 同 ✅ |
| `/admin/kb` | 患者 | 先跳 `/admin/kb` 再被弹回 `/` | 直接落 `/` ✅ |

**教训（已进「已知坑」）**：**登录后的落点必须由角色决定**。任何"带 redirect 回原页"的设计都要先问一句：**这个 redirect 是给谁看的？** 它由未登录访客的入口路径生成，而访客在登录前根本不知道自己是什么角色。

### 2026-09-26 · 知识库管理页接通真实接口（四个 tab 全接，含补的后端维护接口）

**范围**（用户定案「四个 tab 全接」）：文档入库之外，补科室 / 台账 / 术语三块的维护接口。原计划是"另外三个 tab 的接口本轮没做、假数据先别删"，本轮一次做完，页面上不再有任何假数据。

**后端新增**（错误码 5009/5010，接口 5 个）：

| 接口 | 作用 | 关键规则 |
|---|---|---|
| `PUT /api/admin/kb/depts/{id}` | 科室改名 / 位置 / 简介 / 启停 | **科室名全库唯一**——模型输出的是科室名字符串，写库与推荐校验都按名字回填实体（`findByName`），重名会让推荐"取第一条"，页面与看板都看不出异常 |
| `GET /api/admin/kb/mappings` | 映射台账分页 | 只读；科室 id → 名字在后端换好；**一条坏 JSON 不挡住整页**（原样展示 + WARN） |
| `GET /api/admin/kb/terms` | 术语白名单分页（含停用） | 枚举按 code 出（前端认小写编码值） |
| `POST /api/admin/kb/terms/{id}/toggle` | 启停术语 | 翻转后**立即 `refresh()`**，不等 60s TTL——它是 chat 入口的防误杀闸门，点一下就该生效 |
| `GET /api/admin/kb/depts`（改造） | 科室蓝本 + 上传表单共用 | 加 `location`/`intro`/`chunkCount`；切片数经 `kb_chunk` 按 `doc_id` group by 再归到科室（切片表上**没有科室列**，科室归属在文档上） |

`DocPageVO` 顺带提成泛型 `PageVO<T>`，三个分页接口共用一种形状。

**前端**（`KbManage.vue` 整页重写 + `api/admin.js` 10 个函数）：四个 tab 全部走真实接口，空表不渲染表头、批量操作用 confirm 说清后果。**进度按 `progressOf()` 一处集中渲染**——解析中 N%（外部真百分比）/ 正在切分（无数字）/ 写入 x / y（自己的计数）/ 已完成 · N 片，`ratio` 为 `null` 就不画条。列表在有文档跑时每 3s 自刷、跑完自动停表并弹一次完成/失败提示。

**真环境验证**（Edge 无头 + 前端 5174 + 后端 8080，脚本 `frontend/e2e_kb.cjs`）：

| 场景 | 结果 |
|---|---|
| 文档入库 tab | 28 份文档、分页/按科室筛选（18 条）/按状态筛选（空态文案）/重置 全部正确 |
| 上传 → 入库 → 查看切片 → 删除 | md 上传 2s 内跑完、抽屉 2 片正文正确、删除确认文案说清"终止任务+清三处+MinIO"，删后从列表消失 |
| **进度按阶段**（专门脚本 `e2e_kb_progress.cjs`） | PDF 实测轨迹：`解析中 0%` → `写入 0 / 27` → `已完成 · 27 片`，**全程没有推算出来的总进度** |
| 科室蓝本 | 8 行（含停用）、切片数 321 与库内一致；编辑保存生效；**重名拦截**提示「科室名「骨科」已被占用」 |
| 映射台账 | 空态「台账为空：还没有审核回流产出的映射」（表确实是空的，链路 C 后半未动工） |
| 术语白名单 | 72 条；启停往返（已生效 → 未生效 → 已生效）正确 |
| console / page 错误 | **0 条** |

**测试**：后端 `mvn test` **205 用例全过**（原 193，本轮 +12：DeptServiceTest 4 / MedicalTermServiceTest 3 / KbAdminServiceTest 5）。

**踩到的坑（E2E 脚本侧，非产品缺陷）**：Element Plus 关闭的对话框**仍留在 DOM 里**，`.el-dialog .el-input__inner` 按序号取会取到上传弹窗的输入框——第一版脚本因此把科室名改成了「E2E 测试位置」（已改回）。教训：**按 `:visible` 限定 + 按 placeholder 定位，不要按序号**。另：`.el-select-dropdown__item` 同理，要用 `:visible`。

**待办**：

- 映射台账与术语白名单**没有"新增"入口**：台账按设计只读（写入唯一路径是链路 C 的 approve）；术语的人工新增加入仍在等"术语抽取"旁路（LLM 抽取 → 人工确认生效），本轮只做了启停
- 管理端剩余未接通页面只有**审核队列**（依赖链路 C 后半：聚合归桶未动工，`cluster_bucket`/`review_task` 是空表）

### 2026-09-26 · 测试语料端到端实测：解析成果评估 + 检索链路度量 + 修掉两个真缺陷

**背景**：用户在 `application-local.yml` 填了 DocumentMind 的 AK/SK，并在仓库根放了 `测试文档/`（18 份：5 PDF + 1 PNG + 12 MD）。本轮把 18 份全部跑过上传流水线，再走检索链路量召回率/精确率。语料容器用**新建的停用科室「测试语料」**（`dept.id=9001`，`enabled=0`：不进挂号页、不参与推荐校验），不污染 7 个真实科室的语料。

**修掉的两个真缺陷**（都是这轮实测才暴露的，单测与上一次端到端都盖不住）：

1. **版面块按 `index` 单独排序会把多页文档逐条交错**（`DocMindLayoutReader`）。实测确认 `index` 是**页内序号**（第 2 页从 0 重来），真正的阅读顺序键是 **(pageNum, index)**。原代码那句"服务端已排好，这里只是防御性地再按 index 确认一次"，恰好把两页交叠成 A1、B1、A2、B2……——**每块文本都完整，只有顺序不对**，单看一块看不出问题，症状是"召回的内容牛头不对马嘴"。修完重新入库，`多级标题测试文档.pdf` 的切片从 16 条（跨页交错）变成 27 条、顺序与原文一致（仍差 2 个标题：`1.1.1.1` 与 `4.2.1.1` 被上游判成正文，见下方解析损耗）
2. **`contentType` 为空时上传直接 500**（`MinioUtil.upload`）。multipart 的 part 可以不带 Content-Type（客户端按扩展名猜不出来时就省掉），而 MinIO SDK 对它做非空校验、直接抛 `IllegalArgumentException`。透传的后果是上传一份正常文件得到「1000 系统内部错误」，排查要从服务端日志翻栈。回落 `application/octet-stream`，并抽出 `contentTypeOrDefault` 加两条单测钉住（`common` 模块此前没有测试目录，本轮建了）

**分页语义同时确认了**（写进 `.claude/skills/docmind-api/SKILL.md`）：`layoutNum` = **块偏移**、`layoutStepSize` = 取多少块，底层是全篇按阅读顺序排好的平铺列表；**没有总数字段**，只能靠"这一页不满 step"判完（`queryDocParserStatus` 的段落数是解析计数，不等于 layouts 条数）。原有实现按偏移推进是对的，两道防御（大 step + 重复页护栏）保留。

**解析成果（18 份全 success，共 305 片）**：

| 文档 | 切片 | 评价 |
|---|---|---|
| 多级标题测试文档.pdf | 27 | 标题切分基本正确；表格独立成 chunk 且是完整 markdown |
| 列表与图片混排测试文档.pdf | 20 | 无序/有序/嵌套列表、标记符号（• ○ ◆ – *）全部保留 |
| O2 图片型 PDF + 文字截图 PNG | 6 / 6 | **OCR 完整可用**：正文、编号条款、表格全部还原 |
| XX-200 网关产品手册.pdf | 16 | 真实产品手册形态，表格与章节顺序正确 |
| O2-provider-artifact | 9 | 双栏页阅读顺序正确（左栏 5 条 → 右栏 5 条，未交错） |
| 12 份 MD | 4–68 | 原生读路径无异常 |

**解析质量的四类已知损耗**（都在上游 DocumentMind，不在本系统代码里）：

- **标题识别看的是格式而不是编号**：`1.1.1.1术语约定` 与 `4.2.1.1四级` 被判成 `type=text`（正文），于是内容并进了上一节；同时 4.2 文档里的有序列表项「1.准备阶段」反被判成 title。层级树不建（本项目只用边界），这个损耗表现为"个别小节没有独立标题"
- **OCR 字符混淆**：`、`→`丶`、`。`→`·`、`P0`→`PO`。**这条会实打实压检索**：用户按正确的「P0 事故」提问，而库里存的是「PO 事故」，BM25 命中不了，只能靠向量路兜
- **图片没有块标记**：图片内的文字被 OCR 后**直接插进正文流**，没有 `type=image` 占位。图 3-1 那种复杂示意图被 OCR 成「…列表项B子项B-1章节有序列表 图片块→1.第1步232.第第2步图片第3步」这样的乱码，**作为正文进了 chunk**，既污染向量也污染 BM25
- **跨页表格被拆成两块**（1.3 技术规格一分为二），各自成 chunk、共用同一个标题

**检索链路度量**（30 题 + 4 题改述，问法自然、答案在原文可核；每题独立会话）：

| 指标 | 结果 |
|---|---|
| **文档级召回率@N**（正确文档进了送入 Prompt 的 Top5 上下文） | **30/34 = 88.2%**；排除下方被敏感词拦截的 4 题则为 **30/30 = 100%** |
| **Top1 命中** | 27/34 = 79.4%（排除拦截题 27/30 = 90%） |
| **证据命中率**（上下文里真含答案关键词） | 30/34 = 88.2%（**与召回率同值**：只要正确文档进来了，答案就在里面） |
| **上下文精确率**（Top5 里来自正确文档的比例，均值） | 54.1% —— 这是**文档口径的严格下界**：跨小节答案（如同一份制度的不同条款）之外的"同主题他文档"切片不计入分子，而它们对作答实际有用 |
| **端到端作答命中率** | 20/34 = 58.8%（排除被拦截的 4 题则 66.7%）。未命中的 14 题 = **4 题入口被拦（检索没跑）+ 10 题模型以"导诊助手职责"为由拒答**——10 题里证据其实都已正确召回，**不是检索的问题**：语料是企业制度/产品手册，而主提示词是医疗分诊。检索层与回答层必须分开看 |

**顺带挖出的链路 A 问题（未修，属入口过滤，不是本轮范围）**：

- **单字禁止词「滚」被子串命中，误杀「回滚」**：4 道含「回滚」的题**在入口就被拦下、检索根本没跑**（日志：`入口校验：命中禁止词「滚」，拦截`），`hit_count` 还被 +4。种子词表在 `auth/seed/SensitiveWordSeedRunner.java:35`。同类风险：患者说「孩子疼得**打滚**」也会被拦。词表白名单只救"该词本身是医学术语"的情形，救不了"是合法长词的一部分"。建议改词库（`滚` → `滚开/滚蛋/滚出去`）而不是改匹配逻辑——中文没有词边界，改逻辑的代价与风险都大得多
- 「投诉/举报」是观察词，同理会被 `投诉科室位置在哪` 这类正当提问命中（只记不发话术，影响小于禁止词）

**验证**：`mvn test` **193 用例全过**（0 失败）；18 份文档真实上传入库（三处存储一致）；34 题真实走 `/api/chat/message`，逐题核对召回与作答。

**待办（本轮新增）**：

- ~~**管理端知识库页仍是假数据**~~ **✅ 2026-09-26 完成**（四个 tab 全接，见上条施工日志）
- **`测试文档/` 与库里 18 份测试文档**：语料未跟踪、文档留在 `测试语料` 科室下（要清可走 `DELETE /api/admin/kb/docs/{docId}` 或整科室删）
- **图片块处理策略未定**：现在图片内文字直接混进正文。可选：按 `type=image` 丢弃、或转为「[图片] + OCR 文本」独立成块、或只保留有 caption 的图
- **OCR 字符混淆要不要做归一**（`丶↔、`、`PO↔P0` 这类）：属于检索前处理的活，做在查询侧还是入库侧要先定
- **敏感词单字子串误杀**（见上，链路 A）

### 2026-09-26 · 链路 B 上传流水线落地（后端全量，约 1 小时）

**范围**（用户定案）：后端流水线全量；管理端 KbManage.vue 接真实接口放下一轮（接口本轮已就绪）。

**完成**：

1. **格式分流**（`kb/parse/DocFormat`）：`pdf`/`docx`/`png` → DocumentMind（经 llm）；`txt`/`md` → 原生读；`html` → Tika；**其余当场拒绝**（错误码 5003，不建文档）
2. **DocumentMind 客户端**（`llm/client/DocMindParseModel` + `DocMindLayoutReader`）：四个坑全钉住——`processing` 是 0–100、`needHeaderFooter=false`、传流不传 URL；版面块按 `numCol/cells` 判表格（不靠 type）、按 `index` 排序、外层键不认识时扫描兜底
3. **切分三层**（`kb/split/DocSplitter`）：结构切 → 模型切（无标题且够长）→ 递归切（无条件兜底）+ **产出校验**（覆盖率/块数/过碎）；`title` 只来自原文，无标题时回落文档标题
4. **两段式编排**（`async`）：线程池只提交 → `@Scheduled` 扫 parse 任务轮询 → 投回线程池做「取结果 → 切分 → 入库」；第二段**可重入**（只依赖 docId + 任务号）
5. **kb 文档服务**：上传建文档、状态机、**删除补偿**（ES → 向量 → 元数据 → MinIO）、重新处理（先删旧再重建）
6. **接口**：`/api/admin/kb/{docs,tasks,depts}` 上传/列表/进度/切片/重处理/删除；multipart 上限显式配 50MB 并接进全局异常处理器
7. **参数**：切分 3 键入 `sys_config` **白名单**（`chunk_broken` 的修复动作靠它），轮询 2 键入 `sys_config` 不上管理端页面
8. **单测 +57**（kb 34 / async 14 / llm 6 / auth 3）：切分三层与产出校验、格式白名单、txt/md/html 解析、版面块映射、编排阶段推进与失败分类、删除补偿顺序、分批写入的防线复查、切分参数的跨项校验

**推翻/修正的三处（都不是照抄文档）**：

1. **切分三层的实现顺序与文档的字面顺序不同**：文档写「结构切 → 递归切 → 模型切」，但递归切若先无条件切碎长文本，模型切就没边界可给了。实现是「结构切 → 模型切 → 递归切兜底」，三条硬约束仍全部成立；已在架构文档与 CONTEXT.md 写明并解释
2. **Tika 只引 `tika-parser-html-module`**，不引 `tika-parsers-standard-package`：把「Tika 不得接 pdf/docx」这条纪律**下沉到依赖层面**——那个 bundle 会把 pdf/poi 一起塞进 classpath，只引 html 模块则那条降级路径根本不存在
3. **本地解析不用 Tika 探编码**：Tika 探测要额外引编码探测模块，且同一份中文文件在 txt 与 html 两条路上会得到两种结果（一个正确一个乱码）。改为两条路共用 `TextDecoder`（UTF-8 严格 → GBK 回落）；让 Tika 用指定编码，必须设 **`Metadata.CONTENT_TYPE` 里的 charset** 而不是 `CONTENT_ENCODING`——`AutoDetectReader` 只读前者，这是实测出来的

**困难点与解法**：

- **Tika 报「Failed to detect the character encoding」**：只引 html 模块时 SPI 探测器一个都不在 classpath 上。反编译 `AutoDetectReader.detect` 才看清它依次试探测器、再读 `Content-Type` 的 charset 参数，**根本不看 `CONTENT_ENCODING`**
- **`BizException(ErrorCode, detail)` 会把错误码文案拼在前面**（"文档不存在: 文档已删除"），而这串会直接成为管理端看到的失败原因——要自定义人话得用 `(code, message)` 那个构造
- **轮询会重复投递第二段**：解析完成后若不立即推进阶段，下一轮扫描会再挑中它、再投一次 → 切片写两遍。解法是"轮询确认完成后立刻 `moveToSplit`"（阶段即判据），并让 `moveToSplit` 幂等
- **卡住的 split/embed 任务是个死胡同**：进程中途退出 → 任务永远 running → 又因「同一文档同时最多一个 running」而重跑不了。补一条：扫描时把超时没动的 split/embed 任务判中断（retryable），让「重新处理」有路可走
- **单次轮询失败不该判死**：网络抖一下就标 failed，管理员会白重处理一遍（还白付一次解析费）。改为只记 WARN、下一轮再来，真正的兜底是超时判定
- **验证时踩了三个自己的老坑**：Git Bash 里 curl/python 是原生程序，路径要写 `C:/...` 而不是 `/c/...`；`curl` 内联中文 JSON 会乱码，请求体必须落盘；代码块读 SSE 时事件名是 `event:xxx` 而不是 JSON 字段
- **MinIO 凭证险些被我落到仓库外的明文文件**（被权限拦截）：改用「删除接口返回 200 + `文档已删除` 日志在 MinIO 删除之后」来确认清理成功，不再复制凭证

**真跑验证**（中间件全在，后端 8080 + 真实环境）：

| 场景 | 结果 |
|---|---|
| 上传 md（903 字，两级标题 + 超长段落） | 结构切出 3 段、超长段落递归切成 2 片（416 + 405 字，均 ≤ 上限 800），拼回与原文一致 |
| 三处存储 | MySQL / pgvector / ES 一致（12 种子 + 4 新 = 16） |
| 进度 | `stage=embed total=4 done=0` → `stage=done success 4/4`，**显示真实量** |
| 重新处理 | 先删旧再重建，重建后仍是 4 片、seq 1–4 |
| 删除 | ES/向量/切片三处回到 12；文档行消失；再次查切片返回 5000 |
| 不支持格式（.exe） | 当场拒绝 5003，**不建文档** |
| pdf 但没配 DocumentMind 密钥 | `failed` + `retryable` + 原因「文档解析服务未配置，请在 application-local.yml 填写 AccessKey」 |
| **入库内容能否被检索到（链路 B 的收益）** | 上传一份骨科鉴别语料 → 问「颈肩僵硬，手指发麻，握东西没力气」→ **推荐骨科 85%**，`cites` 第 1 条正是刚入库那份文档的标题 |

**顺手收掉进度.md 的阻塞项 #0**：pgvector 存量向量回填（12 条 → `doc_id` 无 NULL）→ step2 收紧 `NOT NULL`。三步走完，`kb_chunk_vec` 迁移**已全部完成**。

**验证**：`mvn test` **188 用例全过**（原 131，本轮新增 57）；真实环境端到端跑通（上表）+ 审查修复后复跑冒烟。

**代码审查（标准轴 + 规格轴并行）查出并修掉的五处**：

1. **僵尸任务无回收（最要紧）**：扫描只捞 `parse 且有任务号` 与 `split/embed`，漏掉**提交途中退出**（进程死在建任务与提交之间）与**本地格式解析时退出**这两类——它们既不可轮询、也不在"卡住"之列，于是永远 running：文档卡在「解析中」，又因为「同一文档同时最多一个 running」而重跑被拒（5005），只能删了重传。**正是规格要堵的那个死胡同**。改为一次扫描覆盖全部 running 任务，分两支：有任务号的 parse 去问外部、其余按"太久没动"判中断
2. **异步解析失败拿不到 fatal**：加密件/损坏件的失败发生在**异步任务**上（提交时看不出来），而 `status=failed` 分支原来硬编码 retryable——规格里「输入问题 → fatal」在最常见的路径上根本不可达，管理端会拿到一个重跑一百遍也没用的「重新处理」按钮。改为：拿响应体外层的 `code/message` 过一遍失败分类，`ParseStatus` 带上分类与人话；拿不到详情才退回笼统的可重试说法
3. **防线① 只在起手查一次**：写入是**分批**的，中途删文档后剩下的批照样写三处，留下"MySQL 活着、检索命中、文档行没了"的孤儿切片（页面上再也找不到、也没入口清理）。改为**每批复查**文档是否还在
4. **`EsChunkUtil.deleteChunks` 的注释与实现不符**：注释写"失败必须抛"，实际 bulk 的逐条错误只记 WARN。改为写清两种失败（传输层抛、逐条错误只记 WARN 且读时会被丢弃）
5. **`KbDocService.delete` 的事务与"顺序保证"看着矛盾**：补注释说明**为什么不能顺手去掉事务**——顺序是按"失败时倒向哪一边"选的（宁可留下"文档在、召回不出"，不可留下"检索有、MySQL 没有"这种静默少召回），事务的作用是让动作**可以重来**：末段失败时元数据回滚、文档行还在，再点一次即收敛；去掉事务则元数据先删、MinIO 对象再无清理入口

另有四处**判定后不修**（记理由）：`KbAdminService.reprocess` 与 `IngestPipeline.start` 各有一道"同时最多一个 running"检查——规则归 async，但前者挡在**删除动作之前**，顺序反了会把正在写的切片删掉，是必要的重复（已补注释）；`KbDocService` 与 `IngestTaskService` 各有一个同形的 `update(id, Consumer)` 小助手——跨模块、实体类型不同，抽公共基类得不偿失；`DocIngestService.requireUsableBlocks` 与 `DocSplitter` 的产出校验同名不同层——一个查"解析抽出多少字"、一个查"切分拼不拼得回"，都已写清；`KbAdminService` 的 `hasRunning` 预检与 async 的权威判定同上。

**待办**：

- ~~**DocumentMind 真调用未验**~~ **✅ 2026-09-26 完成**：18 份测试语料全量入库，外层键为 `layouts`、`layoutNum` 是块偏移、表格 markdown 形态均确认；**并因此查出一个真缺陷**（版面块按 `index` 单独排序会跨页交错），见上一条施工日志
- ~~**管理端知识库页仍是假数据**~~ **✅ 2026-09-26 完成**：`KbManage.vue` 四个 tab 全接；「15 / 38 · 解析中」那条假数据连同整页假数据一起清掉，进度按阶段显示真实量
- **术语抽取仍是旁路未做**（按设计可以先不做）：chunk 的 `medical_terms` 现在是空数组，ES terms 聚合拿不到新词
- **删除补偿失败没有待重试标记**：顺序保证 + 失败即抛 + 日志留痕，但没有载体记"这份文档删了一半"。当前靠人工看日志（重试路径是通的：末段失败会回滚元数据，文档行还在，再点一次删除即收敛——见 `KbDocService.delete` 的注释）
- **MinIO 残留未单独列桶确认**：由"删除接口 200 + 日志位于 MinIO 删除之后"推断，没直接列对象验证
- **入库测试留下的两份文档**：`心内科分诊知识（E2E）` 已删；`骨科分诊补充知识（E2E）` 留库里（它的内容已被检索命中，是链路 B 生效的证据），要清可在知识库页删

### 2026-09-26 · DocumentMind 真跑一次（探针）+ 修正四处判断 + `ingest_task` 加列

**背景**：上一节定「markdown 还是结构化输出」时，两个关键点只能猜（markdown 会不会丢坐标、结构化返回的 schema 长什么样）。用户给 SDK 坐标（`com.aliyun:docmind_api20220711:2.0.14`）后，改成**先真跑一次再定**。

**做法**：`javap` 读 jar 拿到接口形态 → 造一份**合成 docx**（两级标题 + 三行表格 + 页眉页脚，避免上传用户自己的文档）→ 写探针真跑。
探针在 `backend/llm/src/test/java/com/guide/llm/probe/DocMindProbe.java`，**类名不带 `Test` 后缀**，surefire 默认 includes 挑不中它，`mvn test` 不会跑、不产生费用；显式执行 `mvn -pl llm test -Dtest=DocMindProbe -Dsurefire.failIfNoSpecifiedTests=false`。设 `PROBE_JOB_ID` 可复用已有任务号**跳过提交、不再计费**（`getDocParserResult` 是读接口）。

**实测（全部来自真跑，非文档抄录）**：

| 项 | 事实 |
|---|---|
| 调用形态 | `submitDocParserJobAdvance` + `setFileUrlObject(InputStream)` **直接传文件流** → 轮询 `queryDocParserStatus` → 取结果。**不需要 OSS，也不需要 MinIO 对公网可达** |
| 状态 | `success`；**`processing` 是 0–100，不是 0–1** |
| 统计 | `paragraphCount` / `tableCount` / `imageCount` / `tokens` 都现成，可映射 `ingest_task` 进度 |
| 结果 | 两条路：`getDocParserResult`（分页拉版面块）与 `outputFileUrl`（下载文件，OSS 签名**有效 12 小时**）。**都是读接口，反复拉不再计费** |
| 版面块 | `type`(`title`/`text`) / `fontSize` / `pageNum` / **`index`（服务端已排好阅读顺序）** / `markdownContent` / `text`；**普通文本块没有 bbox 坐标** |
| 表格 | **结构化**：`numCol` + `cells[]`，每 cell 带 `xsc/xec/ysc/yec` 网格坐标；markdown 形态是**完整表格** |
| 页眉 | **被识别成 `type=title` 混在正文最前**——必须设 `needHeaderFooter=false`，否则每页页眉都变成一个"标题块" |

**推翻的四处（都是我自己的判断，不是用户的）**：

1. **「必须按 `页 → y → x` 排序」——不必要**：服务端给了 `index`，已排好阅读顺序
2. **「层级最可靠的信号是编号模式，坐标和类型都给不出」——说错了**：`fontSize` 直接给得出（H1=15 / H2=14 / 正文=12）。但**结论不变**——切分只需要边界、不需要层级，是"要不要"不是"能不能"
3. **「markdown 与结构化二选一」——不存在这个取舍**：结构化接口**每块自带 `markdownContent`**，是同一份数据的两个视图（Q18 的前提本身就是错的）
4. **「表格不做特殊处理、可能被切断」——低估了**：表格是结构化的，markdown 形态是完整表格。改为**取 markdown 形态作为一整块入 chunk**，不参与正文切分

**定案修正**：解析输入走 **`getDocParserResult` 拉版面块 + 表格块取 markdown 形态**；提交设 `needHeaderFooter=false`；其余（切分三层、失败分类、术语抽取旁路、模块归属）不变。

**表结构**：`ingest_task` 新增 **`external_job_id`**（外部解析服务任务号）。两段式编排——线程池只提交，`@Scheduled` 扫 running 的 parse 任务轮询，完成后投回线程池——**重启后接着轮询靠的就是它**。同步 `mysql_init.sql` + `migration_20260926_ingest_task_external_job_id.sql` + `IngestTask` 实体。

**困难点与解法**：

- **控制台把中文压成乱码**（Windows 控制台编码），JSON 里的中文全成乱码——按项目已知坑的做法改成**报告落盘 UTF-8 再读**，控制台只留进度
- **探针自身的两个 bug**：完成判据写成 `processing >= 1`（实际是 0–100，第一次跑对纯属运气）；下载的结果文件没加后缀所以没打印内容
- **密钥不能出现在命令行上**（沙箱会拦）：把凭证放进**仓库外的临时脚本**再执行，命令行上只剩脚本路径

**沉淀为 skill**：`.claude/skills/docmind-api/` —— SKILL.md 记「三件事」：三个必踩的坑（`processing` 是 0–100 / `needHeaderFooter=false` / 文件要传流不能传 URL）、三步异步调用与版面块结构、本项目的集成约定（走 `llm` 层、不降级、Tika 只接 html、两段式编排与 `external_job_id`）。探针移到这里 `scripts/` 下（**脱离 Maven 构建路径**，`run-probe.sh` 自己拼 classpath），支持 `PROBE_JOB_ID` 复用任务号——**读接口不重复计费**。以后再动这块代码不用重新摸一遍。

**未核实（留待办）**：DocumentMind 的**计费口径**（jar 里查不到）；**Tika 的依赖体积**（原派的核实 agent 跑偏被停）；`outputFormat` 里 `"json"` 是否合法（传 `["markdown","json"]` 只回了 markdown）。

**验证**：探针真跑通过（合成 docx 解析成功、markdown 与版面块都取到）；`mvn -DskipTests compile` 通过。

### 2026-09-26 · 链路 B 上传流水线设计定案（解析与切分）

**背景**：上传流水线开工前先定解析与切分的方案。原案是「png/pdf/word 交给阿里 DocumentMind，html/txt/md 交给 Tika」，逐轮推敲后收敛——中间推翻了几处，那些才是这次讨论真正的产出。

**定案**：

| 层 | 定案 |
|----|------|
| 格式白名单 | `pdf`/`docx`/`png` → DocumentMind；`txt`/`md` → 原生读；`html` → Tika；**其余上传时拒绝** |
| DocumentMind 定位 | 经 `llm` 适配层（第四个客户端）；pdf/docx/png 的**唯一**解析路径，**不降级**；不可用 → `failed`（retryable） |
| Tika 约束 | **只接 `html`，不得接 pdf/docx**（否则否掉的降级路径会顺着依赖爬回来） |
| 解析产物 | 文本块（类型 + 坐标），**无层级**；必须按 `页 → y → x` 排序（双栏 pdf 不排序会串行，且不报错） |
| 表格 | 不做特殊处理，表格文本原样进正文流（内容不丢、可能被切断） |
| 产物缓存 | **不存** |
| 切分三层 | 结构切（标题块 = 边界、**不建层级树**、`title` 取最近标题块）→ 递归切（**无条件兜底**）→ 模型切（仅无标题部分，**只给边界不给 title**） |
| 模型切兜底 | 结果过**产出校验**（覆盖率 / 块数 / 字数区间），不过 → `failed`（不是 `done` + 0 片） |
| 切分参数 | 走 `sys_config`（`chunk_broken` 的修复动作全靠它） |
| 失败分类 | 依赖故障 → `retryable`；输入问题 → `fatal`；**拿到结果但不合格 → `retryable`** |
| 术语抽取 | **旁路**（失败只记 WARN，chunk 照常入库）→ 可以先不做 |
| 模块归属 | 解析编排 + 切分在 `kb`；`async` 只管线程池与任务表；DocumentMind 客户端在 `llm`；Tika 依赖在 `kb` |

**推翻的六处**（这些才是产出）：

1. **「支持多格式总是好的」被否**：支持但没验证过的格式是**静默的垃圾语料来源**——上传成功、状态"已完成"、然后切出乱码进向量库，没人会发现；而"不支持的格式"当场报错。白名单因此收窄到三种
2. **Tika 原本要接 txt/md/html 三种**：Tika 没有 Markdown 解析器（`.md` 落纯文本分支），txt 同理——这两条等于直接读文件。Tika 最后只剩 `html` 一处用处
3. **「三策略并列」有覆盖漏洞**：结构切（标题 > 2）、递归切（超字数）、模型切（质量低）三个条件**都不满足时无策略可走**——800 字无标题纯文本就是反例。改成**分层**，递归切是无条件兜底
4. **模型切的触发条件原本是「质量低」**：反了——质量低意味着解析出了问题，模型拿到的也是烂文本，**拿最贵的手段处理最该被拒绝的输入**。真正的场景是"**无标题但文本干净**"
5. **「模型切不出来就返回空」不成立**：模型不会说自己切不出来，它会**编一个看起来合理的切分**。兜底只能是**产出校验**——链路 C 根因表里 `structure_fail` 的修复动作写的正是「输出校验」
6. **`title` 不能由模型生成**：它是推荐卡溯源引用要显示的东西，模型起的标题在原文里不存在，溯源就退化成"模型的转述"（= 幻觉）。故结构切定 `title`、模型切只给边界

**未核实的事实**（卡住三项，不影响上面的定案）：

- DocumentMind 的 API 形态（同步/异步、文件怎么传）→ 卡住「异步编排怎么设计」
- DocumentMind 的计费口径 → 决定"每次重入库都重新解析、重新付费"这笔账有多大
- 返回里有没有页号 → 决定「页码溯源」能不能做
- Tika 的依赖体积与 Markdown 支持（原派的核实 agent 跑偏被停，未取得结论）

**验证**：无——本次是**设计定案，未写任何代码**。

### 2026-09-26 · 向量表补归属与正文副本 + 写时补偿（链路 B 前置）

**背景**：上传流水线动工前盘了一遍链路 B 的家底，盘出三件事：① `kb_chunk_vec` 只有 `(chunk_id, dept_id, embedding)`，排查时既看不出一条向量属于哪个文档、也看不到切片内容；② 架构文档与 CLAUDE.md 都写着「chunk 双写 pgvector + ES **在同一事务边界**」，而代码里 pgvector 走独立 `JdbcTemplate`、ES 走 HTTP，**都不在 MySQL 事务里**——这句错述掩盖了一个真实缺陷：ES 一挂，MySQL 回滚但向量留下，全靠 `RagService.fillTexts` 一行 WARN 兜着；③ `kb_doc` 与 `ingest_task` 都记状态，谁管什么，文档里没写清。

**定案**（逐轮推敲后收敛，最初提的三个字段最后只落两个）：
- **正文进 pgvector**：三处都有文本，但副本**只作人工排查用**——检索路径不读它，回填照旧走 MySQL。副产品：孤儿向量从此肉眼可见
- **`doc_id` 进 pgvector**：行自证归属 + 按文档批量清理（带索引）
- **`task_id` 不加**：它唯一的正当理由（同一文档多次运行的产物共存）随「版本管理缓做」一起落空
- **`ingest_task` 保留**：`kb_doc` 是文档**当前态**、`ingest_task` 是**每一次运行**的记录；合并会让重试覆盖上一次的失败原因
- **版本管理、产物快照：缓，不做**

**完成**：
1. **表结构**：`pgvector_init.sql` 加三列 + `idx_kcv_doc`；迁移分两步（`migration_20260926_kb_chunk_vec_payload_step1/2.sql`）——`NOT NULL` 必须先回填，否则已有行加不上约束
2. **写时补偿**（修既有缺陷）：`ChunkIndexService.indexChunks` 内 try/catch，失败用本次内存里记下的 chunk_id 批量回删向量再抛出；只补向量不补 ES（ES 残留同样被回填丢弃，且失败原因往往正是 ES 不可用）
3. **存量回填**：`kb/migration/ChunkVecPayloadBackfill`（`@ConditionalOnProperty` 显式开启，幂等），**只报告孤儿不删除**，日志给出可直接执行的清理 SQL
4. **`PgVectorUtil`**：`upsertChunkVector` 收口成 `ChunkVector` record（6 个参数里 5 个是 String，位置参数太容易串位）；新增 `deleteChunkVectors`（批量回删）、`backfillChunkPayload`、`listChunkIdsWithoutPayload`
5. **文档**：架构文档 §1.2 第 4 条 + 检索数据流 + 链路 B 对齐点（三层防线 → **四层**）+ 6.5 双写一致性；`CLAUDE.md` 两处「同一事务边界」错述；`数据库设计.md` §4.1 + §7（补 `kb_doc` / `ingest_task` 分工）；`CONTEXT.md` 新增「知识库入库」一节 4 个词条

**困难点与解法**：
- **「同一事务边界」是句假话，而且它掩盖了缺陷**：文档写了几个月没人发现，因为孤儿向量在读时被静默丢弃（只留一行 WARN）。**机制描述必须与代码逐字对得上**——写"同一事务"就会让人以为不需要补偿
- **`NOT NULL` 决定迁移必须分两步**：加列、回填、收紧是三件事，塞进一个脚本必然失败。顺带把"报 NULL 约束"的两种成因写进脚本注释——回填没跑完（重跑）vs 孤儿向量（清理），处理方式完全不同
- **`JdbcTemplate.batchUpdate(sql, Collection, batchSize, setter)` 返回 `int[][]` 不是 `int[]`**（每批一个数组），按 `int[]` 接直接编译失败
- **`task_id` 的正当性是自己推翻的**：一开始以「行自证归属」为由推荐加，推到版本管理才发现 `task_id`（哪次运行）与 `version`（哪版内容）是两个轴，前者答不了后者；版本缓做后它就没有场景了

**验证**：`mvn -DskipTests compile` 通过；`mvn test` **131 用例全过**（auth 46 / llm 13 / rag 11 / chat 39 / feedback 11 / stats 9 / admin 2）。**未在真实环境验证**——迁移与回填需在 pgvector 实库上执行。

**待办**：
- **迁移与回填要真机执行**：step1 → 回填 → step2，见 `scripts/sql/README.md`；回填后确认 `SELECT count(*) FROM kb_chunk_vec WHERE doc_id IS NULL` 为 0
- **加列后管理端看不到任何变化**（`KbManage.vue` 仍是假数据）：两个新列目前只有直接连 pgvector 才看得见，这是预期不是漏项
- 版本管理、产物快照：本次缓做，等上传流水线开工时再评估

### 2026-09-20 · 对话页对齐 DeepSeek 三版（标识 / 侧栏底色 / 顶栏移除）

**背景**：患者拿 DS 网页版截图要求继续对齐，四点：① 去掉对话区顶上的「智能导诊」及其样式；② 侧栏与对话区底色要拉开（"稍微暗色一点"）；③ 「智能导诊」挪到侧栏、置于「新的咨询」之上，与折叠按钮同排；④ 收起后只剩一个标识。另要求设计一个 logo。设计稿两轮比选（`前端方案/方案A3-侧栏标识与底色比选.html`、`方案A3-第二轮-标识四方向.html`），第一轮三个"线条图标"被否（"太丑"——太轻太碎、像图标库随手拿的），第二轮四方向（汉字印章 / 角框 / 单据 / 发散）选**丙 · 单据**。

**完成**（全在 `Chat.vue` + `patient.css`，无接口/建表变更）：

1. **新增令牌 `--side: #E9EFEC`**：侧栏底色，比 paper 暗两档。两栏之间**一条线都不画**，靠明度差分开
2. **侧栏标识**：方框里三条由粗到细、由长到短的横线——画的就是推荐卡那组置信度条。展开态 20px、「智能导诊」衬线 15px、收起按钮同行右端（`.p-chat__hd`）；收起态 18px
3. **顶栏整条移除**（桌面 `display:none`）：标题归侧栏抬头、用户区归侧栏底部，对话区顶上不再有一条横线。手机端没有侧栏可挂，仍保留（左「会话」、中标题、右昵称与退出）
4. **收起态精简轨**：248px → 127px 横向一条 = 标识（点它展开）｜ 展开图标 ＋ 图标；原来的「智能导诊」文字入口删掉
5. **加号改半角 `+`**（原为全角「＋」，在该字号下偏大偏上）
6. **侧栏内部的分隔线全部换色**：`--line`(#DDE5E2) 比 `--side`(#E9EFEC) 还亮，画上去等于不存在——天头/行分隔/用户区上边统一改 `rgba(28,43,40,.07~.18)`；选中行实白 `--card`、天头半透明白，靠"白卡片压在灰绿底上"出层次

**困难点与解法**：

- **`fit-content` 不参与 `width` 插值**：收起态原本写 `width: fit-content`，`transition: width .2s ease` 于是完全失效——实测按帧采样只有 `[248, 127]` 两跳。改定值 `127px` + `min-width: fit-content` 兜底后实测 13 帧平滑。**凡是"宽度过渡"，两端都必须是可插值的具体值**
- **底色一换，一批浅色线集体失效**：`--line` 是为 paper 底设计的，放到比它更暗的 `--side` 上直接消失（不报错、只是隔断感没了）。侧栏内部的线要按"比侧栏底暗"重新取，不能沿用全局 token
- **收起态差点丢掉新建入口**：精简轨草图画的是「标识 ｜ 展开图标」两件，照做之后收起状态下开不了新咨询（得先展开）。补回 ＋ 图标——**收纳态可以变简，不能把功能藏起来**
- **第一轮 logo 被否的根因**：三个方案都是"一把散线"（细线、无外框、无块面），放大看还行、放回 20px 就碎。第二轮给每个方向都配了压得住的重心（方框/角框/汉字），并**把四个方向都放进真实侧栏位置比**——44px 大方框里都好看，只有放回实际尺寸才分得出高下

**验证**（Edge 无头实测，1440 / 390 两视口，零 console/page 错误）：
- 底色 `rgb(233,239,236)` = #E9EFEC，对话区 #F7F9F8，侧栏 248 ／ 舞台 1192
- 抬头次序实测：标识行 0–52 → 新咨询 56–96 → 「我的就诊」110 起（标识确在新咨询之上）
- 收起态 127px：轨落在左上 (0,0)、标识 (11,12) 26px、图标 (50,10)、轨内无文字残留；**点标识本身能展开**（sidebox 显示 = true）
- 收起动画按帧采样 13 个中间值，平滑（修复前只有 `[248,127]`）
- 回放态：选中行 `rgb(255,255,255)` 实白、未选中透明、天头 `rgba(255,255,255,0.5)`、书签列在最右 30px、顶栏不可见、无横向溢出
- 手机 390：顶栏仍在且标题为「智能导诊」、覆盖层底色 #E9EFEC、点条目自动收起、无横向溢出
- **既有 `e2e_chat_side.cjs` 全量回归通过**（左栏 / 回放 / 7 根书签正序 / 新咨询 / 昵称入口 / 手机覆盖层），`npm run build` 通过

**文档**：前端设计方案 §2.1 补 `--side` 令牌 + 侧栏内禁用的线色、§2.3 改写顶栏（对话页桌面整条不渲染）、§3.2 补标识与选中行层次、重写精简轨（含 fit-content 那条坑与"不许丢功能"）、§4 修正对话页宽度（1180 → 整页全宽）。

**待办**：
- **收起态没有会话列表**：DS 也没有，但本产品的"翻上次挂的科"入口在侧栏，收起时够不到——要不要给一个折中（悬停展开 / 快捷键）待定

### 2026-09-20 · 问题书签改成悬浮面板 + 滚动条归位（四版，患者两次截图反馈）

**背景**：三版做完后患者给了两张 DS 截图：① 书签要像 DS 那样**展开成面板显示问题正文**，且面板只展示一部分、内部可滚；② 他圈出对话区的**滚动条缩在"对话框"里**、还被面板压住，要求滚动条到窗口最右、把手退到滚动条旁边。

**完成**（同样只在 `Chat.vue`）：

1. **书签 → 悬浮面板**：收起时右缘只有一根 2px `--teal-soft` 细把手（hover 变 teal、加长），鼠标挨到滑出 220px 白卡片——抬头「用 户 问 题」+「N / 总数」，条目 = 问题正文首句（超出省略）+ 右缘小横杠，当前那条文字与横杠变 teal、横杠加长加粗。完全照 DS 的形态，皮肤仍是本方案的直角细线
2. **面板脱离布局流**（`position:absolute`）：顺带把三版记下的"回放时正文横移 15px"一并解决——实测空态与回放态正文列都是 344/1000，一动不动
3. **滚动条归位到窗口最右**：滚动容器 `.p-chat__thread` 从"自己 1000px 居中"改成**整幅宽 + 内边距收窄内容**（`max(32px, calc((100% - 1000px) / 2))`），滚动条因此落在窗口右缘（实测 1440）而不是对话框边缘（旧的 1344）
4. **把手让开滚动条**：滚动条显式定宽 12px 并淡化成纸上的灰线（thumb 用 `border:3px solid transparent` + `background-clip:content-box`，12px 轨道里只画 6px），把手 `right: 12px` 精确停在它左边——两条竖线各归各位、互不打架

**困难点与解法**：

- **容器一全宽，漏掉的那个子元素就会自己铺满**：`max-width: 1000px` 从 `.p-chat__main` 下移到子元素时，只改了 `.p-thread`/`.p-steps`/`.p-composer` 三个——**只读条 `.p-chat__lock` 是第四个直接子元素**，于是它铺满整幅、右端伸到窗口边缘。截图放大到右侧 120px 才看出来（一条绿色短边露在滚动条位置）。**"容器改全宽"这类改动要按模板把子元素数一遍**
- **`scrollbar-width` 和 `::-webkit-scrollbar` 不能并用**：Chromium 认了标准属性后会**整个忽略**那套伪元素，于是"定宽 12px"落空、把手位置又变成猜。只能二选一——要精确对齐就选伪元素
- **headless 量不到滚动条宽度**（`offsetWidth - clientWidth = 0`，Edge 用悬浮式滚动条），所以没法"量出来再定位"，只能先给滚动条定死宽度、再按这个定值排把手

**验证**（Edge 无头，1440／390，零 console/page 错误）：
- `.p-chat__main` **每个**直接子元素的内容左右边界都对齐 344/1344（空态 2 个、回放态 3 个、新咨询态 2 个逐个打印核对）
- 滚动容器右缘 = 1440（窗口最右）；把手热区 1406–1428（右缘距窗口 12px，正好让开滚动条）、视觉线心 1417
- 面板几何 220×276，抬头计数「7 问」；压矮到 150px 后：可滚 = true、渐隐 = true、不越界，滚轮先滚面板（面板 0→60、正文 0）再滚到正文（面板停 126、正文 300）
- 点末条 → 当前条序 7/7、文字变 `rgb(14,124,102)`、正文对应问题被激活
- `e2e_chat_side.cjs`（左栏/回放/书签正序/新咨询/昵称/手机覆盖层）与 `e2e_side_a3.cjs`（底色/标识行/顶栏移除/收起态）**全量回归通过**；`npm run build` 通过

**文档**：前端设计方案 §3.2 重写问题书签六条（面板形态、脱离布局流、内部滚动与滚轮传递、唯一允许阴影处、触屏、定宽滚动条）、补滚动条归位那条（含"子元素要列全"的坑）。

### 2026-09-20 · 对话页 DS 式布局（可折叠侧栏精简轨 + 一键复制）

**背景**：患者给了 DeepSeek 网页版截图，要求交互对齐（侧栏折叠、复制按钮），同时保持临床纸感色板与直角语言不动。半天内按用户六次截图反馈迭代定稿。

**完成**（全部在 `Chat.vue`，无接口/建表变更）：
1. **布局改 DS 式**：整页不再 1180px 居中——侧栏贴浏览器窗口最左，对话内容在舞台中央收 1000px 单列；侧栏与对话区之间的虚线、竖线全部去掉，同为 paper 底自然衔接
2. **侧栏可收成精简轨**：折叠图标在侧栏顶层（新咨询之上，仅桌面）。收起后 248px→横向一条：左「智能导诊」文字入口（点击展开），右图标组（折叠＋＋，一个描边框内相邻、中间分隔线），贴左上角；收起时顶栏标题让位不跳版。状态存 localStorage，宽度过渡 0.2s；手机端不受影响
3. **一键复制**：用户气泡 / AI 回复 / 追问下方复制按钮（桌面 hover 浮现、触屏常显），结论卡「复 制 结 论」常显拼纯文本（科室+置信度+Top3+说明+依据+免责）；clipboard API 失败降级 execCommand，1.5s「已复制」复位

**困难点与解法**：
- **级联静默胜出**：`.p-chat .p-topbar` 曾同时出现在「居中 1000px」组和后面的「重置」组，后者 max-width:none 胜出导致顶栏通栏贯穿、与居中内容列错位——用户截图才发现。**同一选择器不要出现在同一 media 块的两个规则组里；布局改动要对截图验证，不能只看代码**
- 横向精简轨 `height:100% + align-items:center` 会把图标组垂直到屏幕中段——轨高要 `height:auto` 贴顶
- 折叠开关最初放在顶栏，用户定案移到侧栏内部顶层（与 DS 一致）；收起态的开关必须仍在视野里，故精简轨自带一套图标

**文档**：前端设计方案 §3.2（布局、精简轨、复制按钮三条重写/新增）。

**验证**：`npm run build` 通过（无单测面——纯前端样式与交互）。

### 2026-09-20 · 患者端「就诊记录」（只读回放 + 用户问题书签）

**背景**：一个聊天页会先后承载多个会话（实测 e2e_patient 22 个、user1 13 个），此前它们全堆在同一条对话流里，患者看不出边界、也翻不回去；「上次挂的哪个科」只能靠往上滚。顶栏昵称此前是一段不可点的文本。设计稿四个方向比选后定案：**按天折叠的会话列表 ＋ 会话内部的问题书签**（不是会话之间的索引——会话之间靠左目录切，两者不共用一个控件）。

**完成**：
1. **只读接口 2 个**（`admin/controller/RecordController` → `chat/service/RecordService`）：`GET /api/records/sessions`（按天分组，天与会话都最新在前）、`GET /api/records/sessions/{id}`（正文 + 问题锚点 + 结论卡）。路径不在 `/api/admin/**` 下，故按 SecurityConfig 只需登录、不要管理员
2. **结论卡从快照重建**：`rec_top3` 给 Top3 与百分比、`evidence.retrieved` 给溯源注号（注号 = retrieved 顺序），**结论说明不在 guide_record 列里**——只在 `evidence.model_output_raw` 里，用写它时那套 `AnswerParser` 原样解析回来；解析不到就留空，卡片其余字段照常
3. **用户问题锚点**：`chat_message.role=user` 的消息按时间从 1 编号，就是书签；追问（role=question）不编号——它是系统发的，方向相反
4. **前端 `Records.vue`**：桌面 ≥768px 三栏（左目录 ｜ 中正文 ｜ 右缘书签 30），总宽 **1080px**（初版收在 720px，患者看完说两侧空太多，已放宽；目录随视口 `clamp(220px, 24vw, 264px)` 伸缩，正文行宽靠 32px 内边距收着）；手机 <768px 目录收成覆盖层，正文与书签列保留——**同一套视觉语言，只换容器**，不新增桌面专用页面
5. **按天折叠**：某天 >10 次会话折成最新 2 条 + 一行「展开该天全部 N 次」⇄「收起该天」
6. **书签交互**：一根 = 一个用户问题，**自上而下 = 问题 1 → 最新一问**；点它正文滚到那一问并居中、问题行左侧亮 2px teal 竖线；`prefers-reduced-motion` 下滚动改瞬时
7. **书签折起来**：一列放不下时不许撑长页面——超过 8 个先压缩间距（14→7px），仍放不下就书签列自己滚（隐藏滚动条 + 上下 14px 渐隐）；**滚轮落在书签列上滚的是书签列**（没溢出则不接管，照常滚正文），点某一根时它自动滚回视野；是否溢出用 `ResizeObserver` 判（不用一次性计算——换会话、窗口变矮都要重算）
8. **入口**：顶栏昵称改为可点（`.p-topbar__nick`，虚线加下划线，不是按钮样式），进 `/records?tab=booked` 默认停在挂号历史
9. **单测 +8**（`chat/RecordServiceTest`）：按天分组与双向倒序、首句主诉与问题数、多记录只认最后一条、别人的会话按不存在、问题编号、结论卡从快照重建、坏快照不炸
10. **文档**：CONTEXT.md 补「用户问题」「就诊记录」两条（并把「用户问题 vs 追问」的方向差写进 _Avoid_）；前端设计方案 §3.4 新增本页部件规范、§2.3 补昵称入口

**困难点与解法**：
- **`note` 没有落列**：结论说明只存在于证据快照的模型原始输出里。选择"用同一套 `AnswerParser` 解析回来"而不是新增列——**历史不回改**是硬约束，加列只会让旧记录永远是空
- **折叠与"当前打开的那条"冲突**：>10 折成最新 2 条时，正在看的第 7 次会被折没。用户定案「折叠可以展开」，故**不做钉住**，改由右页页脚「第 N 次 / 共 M 次」回答"我在看哪一条"
- **书签视觉 2.5px 但热区 22×14px**：细线好点不中、又不能为了好点就画粗（会破坏书口的节奏）——热区透明放大，视觉不变
- **越权**：详情先查会话再比对 `userId`，别人的会话按「不存在」返回，**不区分"没有这个 id"与"不是你的"**，避免探出别人的会话是否存在
- **书签方向想当然搞反了（患者验收时指出）**：设计时把"最新在上"和目录的倒序当成同一件事，但**目录回答"哪一次"、书签回答"这一次里的哪一句"**——书签是正文的索引，必须跟正文同向（从上往下读）。一个控件的方向不能套另一个控件的方向
- **顶栏标题被挤到昵称上**：加返回按钮后抬头变成三段，左侧 `flex:1` 把标题一路推到右端与昵称粘连。解法是**两侧都 `flex:1`**（返回左 / 标题正中 / 昵称右），标题才真居中

**验证**：`RecordServiceTest` 8 用例通过；Edge 实测三支（点昵称进入 → 挂号历史 → 切全部对话 → 折叠/展开 → 回放 → 7 问题书签跳转 → 手机目录覆盖层），**零 console / page 错误**；接口与页面对照：`totalSessions=22 / totalBooked=5` ↔ 页面 22 条 / 折叠条 / 5 条挂号，卡片 `心血管内科 88%` + 注1–注5 与库中 `rec_top3`、`evidence` 逐字段一致

**验证（改方向 / 折起来 / 加宽后复测）**：书签编号实测 `[1,2,3,4,5,6,7]` 递增，点最下一根激活第 7 问、点最上一根激活第 1 问；把书签列压到 150px 后 `is-scroll=true`，滚轮 ↓300 **只滚书签列**（scrollTop 0→43，正文不动），折起来状态下点最后一根自动滚进视野（`lastInView=true`）；宽度实测 1440 视口 1080px、1000 视口铺满且目录 240px、390 视口原样（split 390 / 书签列 30 / 无横向溢出 / 有「目录」按钮）；抬头三段实测居中且不重叠（桌面左 411px 右 455px、手机左 67px 右 111px）；**零 console / page 错误**

**待办**：
- **阈值是拍的**：一天 >10 折成 2 条、书签 >8 压缩间距，这几个数没有依据，等真实数据量再定
- **展开后到顶没有**：一天几百条时"展开全部"会拉成长卷，需要上限或分页
- **本页无埋点**：是否要记一个 `records_view` 触点（链路 C 现只有三触点）待定
- **对话页是否一起放宽**：本次只放宽了就诊记录页，对话页仍是 720px 居中（要不要跟，等定）


### 2026-09-20 · 首页会话栏（左栏目录 + 只读回放 + 问题书签）

**背景**：就诊记录此前只存在于 `/records` 整页——想看上次挂的什么科，得从对话页跳走再跳回来。患者定案：**把会话目录移到首页**（参考 DeepSeek／豆包那种左栏随手切的手感），聊天和翻旧账在同一屏里完成。

**完成**：
1. **左侧会话栏**（`Chat.vue`，桌面 248px 常驻 / 手机覆盖层）：抬头「我 的 就 诊」+ 次数 + 两个 tab + 按天分组（>10 次折成 2 条，可展开）+ 底部「＋ 新 的 咨 询」。**部件直接复用就诊记录页那套**（`.p-day` / `.p-i` / `.p-fold`），不重画
2. **点会话 = 只读回放**：`RecordService` 的详情转成对话区能渲染的条目（`toReplayEntries`），与实时 SSE 出来的形状对齐，**同一套模板渲染**——`shown = 回放条目 或 本次对话条目`，只有卡片按钮和流式光标在回放时收起来
3. **只读条**：回放时输入区换成「历 史 记 录 · 只 读」+「开始新的咨询」。一次会话 = 一次就诊，翻旧账不能往里写字
4. **问题书签**（回放时出现在右缘）：逻辑与视觉整套搬自就诊记录页——自上而下 = 问题 1 → 最新，放不下时折起来可滚
5. **「＋ 新的咨询」**：撤回放 + `chat.reset()` + 光标落进输入框；每轮对话结束自动 `loadSessions()`，新会话自己冒出来并高亮
6. **输入区改成一整块「书写区」**（患者拿着 DeepSeek 网页版截图要求对齐）：card 底 + 1px 描边，上写字、下排一行——左「分诊建议，不能替代医生诊断」，右 32px 方形发送按钮（白色向上箭头图标，不再是「发送」二字）；聚焦时**整块**变 teal 描边；输入框随内容长高（120px 封顶后自己滚）。**学的是排布不是皮肤**：不圆角、不加阴影，直角与细线守住纸感
7. **侧栏对齐 DeepSeek**：「＋ 新的咨询」从底部移到最上头（会话一多底部够不到）；新增底部用户区（昵称 + 退出），桌面顶栏因此只剩标题；手机没有侧栏，用户区仍在顶栏
8. **删掉独立的免责行**：收进输入区左下角——医疗产品该一直看得见，顺带省掉空状态那单独一行
9. **纸面 1180px**（左 248 栏 + 右 930 舞台），边界画在最外层的 `.p-chat__body` 上
10. **文档**：前端设计方案 §3.2 补左栏/回放/书签/输入区规范、§2.3 说明顶栏用户区归属、§4 按页列宽度；CONTEXT.md 补「回放」词条并改写「就诊记录」两条入口

**困难点与解法**：
- **回放不能污染实时对话**：回放条目另存本地 `replay`，不塞进 Pinia 的 `chat.entries`——否则「看历史」会把用户正在进行的对话冲掉；点「开始新的咨询」回来时原对话还在
- **`margin: 0` 把居中的下半截吃掉了**：媒体查询里给区块统一写 `margin: 0` 去覆盖全局限宽，顺手也把免责那行的 `margin-bottom: auto` 抹了，空状态于是被推到底部（实测距底 48px 而非居中）。**批量覆盖要负责列全例外项**——单独把 `.p-chat__disc` 拆出来写
- **书签在实时对话里没有意义**：实时只有一问，做索引是空的。只在回放时显示，也就只在回放时算溢出
- **空状态下输入区在纸中间，却在纸上画了条假纸边**：`.p-composer` 的 `border-bottom` 本来是给"贴底输入区"收纸口用的，居中之后它悬在纸中间，横看就是纸的下沿——空状态时单独关掉
- **对齐参考产品，学排布不学皮肤**：DeepSeek 的输入卡片是圆角、有阴影、深色底的。直接抄会把「临床纸感」整套抹掉。取的是**结构**（上写字下按钮、发送用图标、新对话在顶部、用户区在侧栏底部），皮肤仍用本方案的直角＋细线＋teal

**验证**：Edge 两视口实测——版面 1180／248／930；左栏 6 条、两个 tab、按天分组「09-20　6 次」；点会话后高亮该条、气泡与卡片按原顺序铺开、**输入框消失换成只读条**；7 根书签编号 `[1..7]` 正序，点最下一根激活第 7 问；点「开始新的咨询」→ 空状态回来、只读条消失、光标落进输入框、左栏高亮清空。空状态仍居中（输入区 top=561、距底 220）。**发消息后左栏自动刷新**：抬头 27 → 28 次，新会话「头疼头晕 1 问 14:36」出现在首条并高亮；发送后输入区贴底（距底 0）、流程条出现。手机端 390 覆盖层开合正常、无横向溢出。**零 console / page 错误**

**同日追加：删掉 `/records` 整页**（患者定案"记录留在首页就行了，首页能看不用其他的了"）。`Records.vue` 删除，路由改成 `redirect: '/'`（老链接不留 404），配套的 4 支 `e2e_records*.cjs` 一并删除；昵称的落点从"跳页"改为"切到挂号历史 + 打开侧栏"（桌面侧栏常驻，同一个函数两处通用）。

**待办**：
- 左栏两个 tab 的切换目前只过滤列表，不改变右侧（原记录页里切 tab 会顺带切当前会话）——要不要对齐，等看实际用起来的感觉

### 2026-09-20 · 对话页空状态「接诊单」

**背景**：对话页一条消息都没有时，整片对话区只有一行灰字「说说哪里不舒服，我来帮您分诊」。患者看不出该说什么、说多少（实测很多人只发"头疼"两个字就等着出结论，模型拿不到部位与时长只能追问），屏幕也空得厉害。出稿三方案（接诊单 / 三步预告 / 复诊回顾）比选，患者选**接诊单**。

**完成**：
1. **接诊单**（`Chat.vue` 空状态）：eyebrow「分诊台」→ 24px 大衬线标题 → 「说清三件事，分诊会更准」→ 三个待填单据槽「部位 / 多久了 / 什么感觉」→ 一行示例（teal-soft 左竖线，与追问气泡同一套语言）→ 「常见主诉」6 个 chip → 虚线分隔 + 免责「分诊为就诊建议，不能替代医生诊断」
2. **chip 只填不发**：点一下把主诉填进输入框并聚焦；已有内容用「，」追加。**不直接发送**——患者还能补一句"还伴着恶心"（分诊更准），也不至于误触烧掉一次模型调用
3. **空对话时输入框跟着欢迎语走**（患者反馈"空对话的感觉有点奇怪但说不出"后改）：接诊单 + 输入区 + 免责当成一整块在顶栏以下居中，同时不显示流程条；发出第一条后立刻回到常规排布（输入框贴底、流程条出现）。用上下 `auto` 外边距而不是 `justify-content: center`——空间不够时它退化成 0，不会把顶部裁掉
4. **桌面纸面 720 → 1000px**（同一反馈）：720 在 1440／1920 屏上两侧各空 360／600px，像张纸条贴在墙中央；文字区块内边距同步放宽到 32px 保住阅读行宽。手机端一行没动
5. **文档**：前端设计方案 §3.2 补空状态与 chip 行为规范、§4 补放宽理由

**困难点与解法**：
- **三个槽位一度做成了按钮的样子**（card 底 + 描边 + hover），会被当成可点的快捷入口。改成**只有一条虚线底边的"空格"**——它是"这里该填什么"的提示，不是控件；真正的可点入口是下面的 chip
- **桌面上三条虚线被 flex:1 拉成一整行**，加了 `max-width: 430px`；手机上（390px）不触发，两端同一套代码
- **"空对话别扭"说不清在哪**：不是字太少，是输入框跟欢迎语被几百像素隔开。患者见过 DeepSeek／豆包，潜意识知道空对话该是什么样却指不出差别——所以「参考 XX 网页」这种说法比"再饱满一点"有用得多
- **纸面边界不能挂在会收缩的区块上**：空状态下对话区收缩成内容高，左右 1px 细线若还挂在它身上，纸就断成两截。边界收归 `.p-chat__main` 统一画

**验证**：Edge 三视口实测（1920 / 1440 / 390）——纸宽 1000（手机 390 原样）、文案 9 项逐字核对一致、6 个 chip 高度均 **44px**（触控目标达标）、无横向溢出；点「肚子疼」→ 输入框得到 `肚子疼` 且焦点落到输入框、再点「心慌胸闷」→ `肚子疼，心慌胸闷`；**全程 `/api/chat` 请求数 = 0**（证明 chip 只填不发）。排布切换实测：发送前输入区 top=556（屏高 900，居中）、流程条不渲染；点 chip 回车发出后输入区 top=829 **距底 0**、流程条出现、整页不可滚。纸面边界用放大截图像素确认（x=220 竖线贯穿到底）。挂号页随 `.p-reg__body` 一起放宽，`e2e_patient` 全链路（导诊 → 推荐卡 → 7 科室 → 挂号成功）重跑通过。**零 console / page 错误**

**待办**：
- **6 个常见主诉是拍的**：没有依据，等真实主诉分布出来再换（可考虑从 `guide_record` 的 rec_top3 反推高频科室对应症状）

### 2026-09-20 · 敏感词按用户累计处置（警告 / 禁言）

**需求**：对话过程中记录触发的敏感词/观察词；**1 小时内**敏感词触发 10 次警告、30 次禁言，观察词 25 次警告但**不禁言**；用户管理页显示对应时间内触发词的次数；阈值与时长要可配置。

**定案**（用户确认）：禁言**定时自动解除**（不复用封禁状态）｜计数**按命中词次**（不是消息数）｜警告走**对话内固定话术**。

**完成**：

1. **表结构**：`user.mute_until`（禁言截止，NULL/过期=未禁言）+ 新表 `user_violation`（处置留痕：级别/触发类型/命中词次/窗口/阈值/截止）；`filter_log` 补 `(user_id, matched_at)` 索引；`sys_config` 新增 5 个键（窗口 60 / 禁止词 10·30 / 观察词 25 / 禁言 60）。迁移脚本 `migration_20260920_user_violation.sql`，重复执行不覆盖已调过的值
2. **auth**：`ViolationLevel` 枚举、`UserViolation` 实体+mapper、`UserViolationService`（禁言状态读写、警告/禁言留痕、窗口内是否已告警、立即解禁）；`UserAdminService.unmute`；用户行 VO 补 `muteUntil` 与统计挂载位
3. **chat**：新增读端口 `port/SensitiveHitCounter`（feedback 实现，与 rag 的 `ChunkTextProvider` 同模式）；`SensitiveGuard` 改为「先判禁言 → **收集全部命中** → 先计数再落库 → 处置」；新增 `SensitiveViolationService` 承载阈值判定；`prompts.yml` 加 `warn-reply` / `mute-reply`（后者必含 `{minutes}`，启动校验）
4. **feedback**：`SensitiveHitQueryService`（实现端口 + 管理端按词明细）
5. **admin**：`UserRiskService` 编排「用户行补窗口统计」，新增明细与解禁接口
6. **前端**：患者端新增 `notice` SSE 事件与独立警告气泡；管理端敏感词库 tab 加「处置规则」面板、账号管理加触发次数列/处置列/明细弹窗
7. **单测 +41**：auth 15（处置 9、参数白名单与跨项校验 4、用户管理 2）；chat 20（入口校验 `SensitiveGuardTest` 10、阈值判定 `SensitiveViolationServiceTest` 10）；feedback 6（窗口计数与明细）。全后端 **123 用例通过**

**困难点与解法**：

- **`LambdaUpdateWrapper.set(SFunction, …)` 在纯单测里必炸**（`can not find lambda cache`），而 `LambdaQueryWrapper.eq` 不炸。用探针测试定位到：`Update.set` **调用当刻就解析列名**，`eq` 之类条件的列名延迟到生成 SQL 时才解析；纯单测没有 Spring 注册 TableInfo，所以只有"lambda 更新语句"会抛。解法：`MpTableInfoTestSupport.init(User.class)` 在 `@BeforeAll` 里显式注册（不必为它拉整个 Spring 上下文）
- **阈值配反会静默失效**：禁言阈值 ≤ 警告阈值时，判定"先看禁言线"，警告这一档**永远不会触发**，页面却一切正常。解法：保存时做跨项校验（只在本次提交涉及这两个键时校验，避免改无关参数被历史配置拦住）
- **警告话术不能走 delta**：delta 的语义是"同一气泡的增量"，提示会与紧随其后的答案粘成一段话、前端再也拆不开。解法：新增独立 `notice` 事件，前端另起一泡
- **禁言期不累计违规**：否则用户随便发几句就自我续禁，禁言期滚动延长、永远出不来
- **禁言也要按窗口判重**（自查时发现）：窗口内累计次数不会因为一次禁言而清零，不判重的话管理员「立即解除禁言」后，用户下一条消息就会被旧计数重新禁上，解除形同虚设。现在警告与禁言都是"同一窗口同类处置只做一次"

### 2026-09-20 · 根因清单定案：存小写 key、页面显示中文

**背景**：做看板时发现文档与实现打架——`数据库设计.md` 写「根因 key 存小写」，而前端硬编码的是中文名本身，看板准确率口径又依赖「患者挂错」这个中文字面量（改文案就会静默失效）。用户定案：**全按小写 key 存，页面显示中文**。

**完成**：
1. **新增 `feedback/enums/RootCauseKey`**（7 项字典）：**key 由常量名派生**（`name().toLowerCase()`），不另设字符串字段——从根上杜绝「常量名与编码值不一致」的漂移；因 `causes` 是 JSON 串、本枚举不参与 MyBatis 装载，故无 `@EnumValue`
2. **`DashboardService`**：`CAUSE_PATIENT_WRONG` 改为从枚举取 key（不再写字面量）；分布按 key 聚合、下发中文名；**字典外的 key 原样显示并记 WARN**——统计可以少一行解释，不能把数据藏起来
3. **`Review.vue`**（页面壳）：清单由中文名数组改为 `{key,label}`，chips 存 key、显示 label；桶内记录展示走 `causeLabel()`；假数据 `['检索失败']` → `['retrieval_fail']`
4. **单测 +6**：新增 `RootCauseKeyTest`（5 个）覆盖 key 派生、label 非空、`patient_wrong` 钉死、`fromKey` 容忍大小写且未知返回空、`labelOf` 回落原值；`DashboardServiceTest` 补「字典外 key 原样显示」

**困难点与解法**：
- 项目有 `EnumConventionTest` 要求「枚举编码值与常量名小写一致、且有且仅有一个 `@EnumValue`」——本枚举不参与装载，登记进去必然失败。解法：让 key 从 `name()` 派生（**不可能不一致，比测试更强**），并在枚举 javadoc 写明「不要登记进 ENUM_TYPES」
- 「患者挂错」的排除仍用 LIKE 粗筛 + 数组元素精确比对：`patient_wrong_pending` 这类新 key 会被 LIKE 命中，但不应算作排除项（已有用例覆盖）

**验证**：`mvn test` **82 用例通过**（auth 31 / llm 13 / rag 11 / chat 11 / **feedback 5** / **stats 9** / admin 2）；`npm run build` 通过

**文档**：`数据库设计.md` 例外条文重写（存 key / 显示 label / 唯一定义源）；`总体架构与链路设计.md` 链路 C 根因选项表加「存 key」列 + §7.2 根因分布口径改写；前端设计方案无需变更。

### 2026-09-20 · 管理端数据看板接通（stats 纯读聚合）

**背景**：看板页此前是假数据。本次按链路 C「看板实时反映准确率」的既定口径落 stats 聚合，把三条容易算错的口径写死在后端（前端只显示）：**准确率只算已确认挂号的记录**（模拟挂号是产品引导、非数据保证，不做完整性假设）、**低置信度不进准确率**（它进盲区榜）、**miss 口径排除已标「患者挂错」**。

**完成**：
1. **准确率口径**（`stats/service/DashboardService`）：分母 = `actual_dept_id` 非空 且 `low_confidence=0` 且未被标「患者挂错」；分子 = `top1_hit=1` / `top3_hit=1`。**样本为 0 显示「—」而不是 0%**（0% 会被读成"全错"）；环比给**百分点差**，上期无样本时明说"不可比"而不伪造数字
2. **每日趋势**：与 KPI 同口径，按 `DATE(created_at)` 分组（命中=top3_hit=1 / 未命中=top3_hit=0）；缺数据的日期补 0 不让图错位；柱高按窗口内最大值归一化并留 20% 余量（真实日量可能远超 100 条，直接当百分比会溢出容器）
3. **错误根因分布**：读 `root_cause` 最新归因（`root_cause_log` 只审计不聚合），按**标注项**计数、降序、百分比 = 项数/全部标注项；坏 JSON 跳过并 WARN
4. **「患者挂错」排除的精确判定**：LIKE 只做粗筛（JSON 串包含），命中后**再按数组元素精确比对**——否则「疑似患者挂错待确认」这类新文案会被误伤成排除项
5. **最近导诊记录**：主诉 = 该会话第一条患者消息（按页批量查一次，不在循环里回表）、截断 40 字；科室名走 `dept` 表直查而非 `DeptService.listEnabled()`（停用科室的历史记录也要显示得出名）；结果四态未反馈/top-1/top-3/**未命中**——top-1 未中但 top-3 中属于排序精度问题、不是系统错，不显示成"未命中"
6. **前端**：`Dashboard.vue` 假数据全替换（KPI 环 + 趋势堆叠 + 根因分布 + 记录表 + EP 分页），空态分别给"还没有已确认挂号的记录 / 暂无归因数据 / 还没有导诊记录"
7. **单测 8 个**（`DashboardServiceTest`）：口径百分比与环比、无样本显示「—」、上期空不可比、趋势补零与星期标签、根因分布计数与坏 JSON、挂错精确排除、记录四态与截断、空页不发多余查询

**困难点与解法**：
- 第一版测试数据（32 字主诉）没超过 40 字截断阈值，断言 `endsWith("…")` 直接失败——**测试用例本身写错了**，把主诉加长到 50+ 字并改成"短于原文 + 以省略号结尾"的断言
- 环比在"上期无样本"时数学上不可比（除数为 0），要么显示 "—" 要么伪造一个数字；选择后者会在演示里被追问，故显式输出"不可比"
- stats 需要读 `dept` 表拿科室名 → 新增 stats → kb 只读依赖（原 pom 只声明了 chat/feedback），已写进架构文档 §3 硬性规则

**验证**：`mvn test` 全后端 **76 用例通过**（auth 31 / llm 13 / rag 11 / chat 11 / **stats 8** / admin 2）；`npm run build` 通过

**待办**：
- 联调验证：看板加载（KPI/趋势/根因/记录）、无数据时的空态、分页翻页
- 根因分布要等审核页能标注根因后才有数据（当前 `root_cause` 为空表 → 页面显示空态，这是预期）

### 2026-09-20 · 管理端 LLM 配置页接通（参数白名单 + 连通性探测）

**背景**：管理端四页（LLM 配置 / 数据看板 / 审核队列 / 知识库管理）此前全是假数据，后端一个 controller 都没有。用户指定先做「用户端已实现、管理端未落实」的部分，知识库管理（链路 B 上传流水线）暂缓。本次最小闭环先落 LLM 配置。

**完成**：
1. **运行时参数读写**（`auth/service/SysConfigAdminService`）：受管键、类型、取值范围、默认值、说明全在代码侧白名单（`ParamSpec`），管理端只能改这些键的值，不在白名单的键直接拒（错误码 2008）；按类型二次校验（int 范围 / 小数 0–1 / 布尔只认 true/false），不信任前端；值缺失回落默认值，首次部署不灌 sys_config 也能开页面
2. **保存语义**：先全量校验、再统一写入——"有一项非法则一项都不落库"是校验阶段的自然结果，不依赖事务回滚；写完 `SysConfigService.refresh()` 立即失效缓存
3. **模型接入信息**（只读）：模型名/地址/维度读 `llm.*`，**API Key 只回"是否已配置"，不回值也不回掩码串**（在线改密钥会让密钥进库+进审计日志，刻意不做）
4. **连通性探测**（`llm/service/LlmDiagnosticService`）：三路各发一次最小请求（一句问候 / 一条向量化 / 一对候选精排），人工触发同步返回、不走 SSE 线程池；每路独立兜异常；向量维度不一致单独判失败；失败原因**先按密钥替换再截断**（上游响应体可能回带 Authorization 头）
5. **前端**：`api/admin.js` 补 4 个接口；`LlmConfig.vue` 假数据全替换（模型状态 + 密钥状态 + 探测结果/失败原因 + 参数表单 + 保存回读规范化值），沿用 admin.css 既有部件类，交互反馈统一 ElMessage
6. **单测 +17**（auth 10 + llm 7）：白名单拒绝、越界/非数字/空值、布尔归一、缺行插入 vs 有行更新、整批校验不落库；探测三路互不影响、密钥脱敏、空返回判失败、维度不一致、超长截断、密钥只回布尔位
7. **文档**：总体架构 → v1.8 新增 §7 管理端控制台；§5 补管理端可调范围说明

**困难点与解法**：
- 原实现用 `@Transactional` + 边校验边写，靠回滚保证整批语义，且 `refresh()` 在事务提交**前**执行——别的线程会在窗口期用未提交的旧值把缓存重新填上。改为「先全量校验、再统一写入」，两个问题一起消掉（详见 §7.1）
- 参数日志里的"原值"若走 `SysConfigService.get()` 会受缓存影响，可能把新值当旧值打出来 → 原值改由 upsert 内那次 `selectOne` 返回

**验证**：`mvn test` 全后端 68 用例通过（auth 31 / llm 13 / rag 11 / chat 11 / admin 2）；`npm run build` 通过

**待办**：
- 联调验证：页面加载（模型+参数）、改参数保存后导诊立即用新值、连通性探测三态（全通/部分失败/密钥未配）
- 下一步：数据看板（stats 聚合，不依赖新流水线）→ 审核队列（依赖链路 C 后半）→ 知识库管理（链路 B，体量最大）

### 2026-09-20 · 规则门槛扩展（无效输入全轮次拦截 + 追问预算护栏）

**背景**：原规则门槛只在首条主诉生效（`isFirstTurn && tooVague`），第 2 轮起患者答得再模糊也会直接进检索+模型。评估过两种「扩展到非首条」的口径——`字数 < N 或 未命中术语`、`未命中术语即拦`——对照白名单实际词形后都否决：白名单是书面医学词（`PART_TERMS` 31 个 / `SYMPTOM_TERMS` 42 个，无「疼/肚子/发烧/拉肚子」等口语别名），拿它当应答轮闸门十条里误杀八条（「三天了」「左边，按压更疼」「肚子疼」「没有发烧」「还拉肚子」全不命中）；且规则门槛位于 forceConclusion 判定之前，一旦永久拦截，`ask_round` 只涨而永远走不到检索分支，会话卡死在「回答 → 被问同一句 → 再回答」的循环里。

**完成**：
1. **确定性无效输入全轮次拦截**（`SufficiencyRule.noSignal`）：语气词/寒暄/说不出/纯符号。判定口径 = 「整条（剥掉尾部标点符号后）精确等于无效词表，或整条不含任何文字与数字」；**整条精确匹配、不做子串**（否则「没有发烧」「不知道怎么说」会被误杀），词表刻意不收「没有/是的/对/好的/行」这类否定与确认应答（它们在追问上下文里正是有效回答），只含数字的（如「38.5」体温）不算无效
2. **追问预算护栏**（`ChatService.ruleGateReason`）：`askRound >= ask_max_rounds` 时规则一律让路——这是防死循环的硬约束，不是可选优化
3. **应答轮不叠加内容门槛**（明确取舍）：追问的回答天然短且口语，语义判断交给持有完整历史的第二层（模型合并判定）
4. **单测**：新增 `SufficiencyRuleTest` 6 个用例（无效输入识别 / 尾部标点不改变判定 / 否定与确认应答不误杀 / 口语短回答不误杀 / 首条门槛口径不变 / 话术取自配置），全过

**困难点与解法**：
- 多模块 `-am` 配 `-Dtest=某类`，在没有匹配用例的模块上会直接失败；`-DfailIfNoTests` 在 surefire 3.1.2 已失效，正确属性是 `-Dsurefire.failIfNoSpecifiedTests=false`
- PowerShell 调 `mvn.cmd` 传 `-Dsurefire.xxx=false` 会被拆成 `.failIfNoSpecifiedTests=false`（报 Unknown lifecycle phase），整个参数加引号才通
- 白名单口语缺口是**既有问题**：首条门槛同样在吃（「肚子疼」当首条会被判笼统），但首条规则只生效一轮、第 2 轮自动放行，问题被掩盖——搬到应答轮才会暴露成永久误杀

**待办**：
- 联调验证两件事：无效输入（「嗯」「在吗」）在第 2 轮也被拦；有效短回答（「三天了」「左边，按压更疼」）不被拦
- 可选（若要给应答轮加内容门槛的前置条件）：补白名单口语别名（肚子/发烧/拉肚子/嗓子/疼/痛/闷/胀等），补完再评估

### 2026-09-19 · 可观测性（全链路日志）+ 提示词外置 + 敏感词种子

**完成**：
1. **链路 A 全链路日志**：入口校验（命中词/动作）→ 规则门槛 → 检索各阶段（双路召回条数与最高分、RRF 候选与双路命中数、精排逐条 score/chunkId/标题、各段耗时）→ 模型生成（首字延迟、总耗时、输出字数、分片数）→ 结论解析（判定、模型自报置信度与校验结果、Top3、引用注号）→ 结论落库（recordId/科室/置信度/低置信度阈值判定/证据条数），一行一个关键数据
2. **轮次标记**：MDC 键 `turn`（`sXXXXXX-rN` = 会话后 6 位 + 本轮序号），跨 chat/rag/kb/llm 各层日志按它归组；console 与 `logs/guide.log` 双写，图案见 application.yml `logging.pattern.console`
3. **提示词外置**：新增 `admin/src/main/resources/prompts.yml`（导诊主提示词 / 查询改写 / 模板追问 / 拦截话术），经 `spring.config.import` 引入，`PromptProperties` 绑定并在启动时校验必填项与占位符（缺 `{knowledge}` 这类占位符会直接启动失败，避免静默降质）
4. **敏感词库种子**：`SensitiveWordSeedRunner` 幂等补 15 条（禁止词 10 / 观察词 5），按词去重不覆盖管理端已有数据

**困难点与解法**：
- 日志格式改了 console pattern 后，MDC 为空的行会显示 `[]`——刻意保留，便于区分「有轮次上下文」与「系统启动期」日志
- 提示词里的 JSON 示例本身带花括号，占位符若用 `{dept}` 这类形式会与示例冲突 → 占位符只取 `{knowledge}/{depts}/{marker}/{extra}/{dialogue}` 五个不会出现在业务 JSON 里的名字
- 结论分隔符是**代码侧协议**（AnswerParser.MARKER 要用来切分输出），提示词里用 `{marker}` 引用而不是各写一份，避免两边不一致

**实测**：
- 「你们狗屎医院，看个病这么慢」→ 命中禁止词「狗屎」拦截，未调模型（此前该词不在词库，一路放行到模型——词库需管理端维护或种子补全）
- 「我要投诉你们医院」→ 命中观察词「投诉」放行 + `filter_log(watched)` 留痕，模型正常受理并引导到投诉渠道
- 「爬楼梯胸口发闷伴左肩酸」→ 完整日志链：向量 10 条（最高 0.7402）/关键词 5 条（最高 4.2324）→ RRF 双路命中 5 条 → 精排 #1 score=0.9277《劳力性胸闷胸痛的识别与鉴别》→ 首字 1709ms / 总 2684ms → 置信度 0.88（校验通过）→ 落库心血管内科

### 2026-09-19 · 链路 A 代码审查修复（同一批实现，审查后补）

**背景**：链路 A 实现完成后跑了一轮多智能体代码审查（正确性 + 复用/简化/效率/边界/规范），共 30+ 条候选，逐条核对后确认并修复 15 处，其余为设计取舍或已记录遗留。

**已修（按影响面）**：
1. **SSE 错误路径全断**（前端）：后端把参数校验失败包成 Result JSON，而接口 produces 是 `text/event-stream`——前端 Accept 只声明事件流会让 Spring 协商失败返回 **400 空体**；即便拿到 JSON，客户端也只按事件流解析，错误被当空流吞掉，患者只看到一个空白气泡。改为 Accept 同时声明 JSON + 按 content-type 分流 + 收流未收终态即报「连接中断」
2. **异常原文直通患者**（chat）：`BizException.getMessage()` 里有上游响应体（含请求 id、Key 片段）与配置项名，改为白名单式文案映射，原文只进日志
3. **敏感词防误杀判据错**（kb/chat）：白名单含「心/头/手」等单字，子串判定让「黑心医院」永远拦不住；改精确匹配 `isTerm`
4. **规则硬门槛被拦截消息废掉**（chat）：首轮判定原按消息条数，敏感词拦截也落库→拦截后永久失去模板追问；改状态机判定（askRound=0 且 hasResult=0）
5. **线程池拒绝策略反噬流式**（chat）：CallerRunsPolicy 在 Tomcat 线程里同步跑完模型生成，emitter 未交给 MVC，逐字渲染整体失效；改 AbortPolicy + 明确 error 事件
6. **术语/配置缓存永不失效**（kb/auth）：文档承诺「管理端即时生效」，实际永久缓存且 refresh 无调用者；加 60s TTL
7. **ES 索引只在种子装载时创建**（kb）：种子默认关时索引永不创建，写入回滚而检索静默降级（表现为 BM25 总召回 0）；新增启动即建的 EsIndexInitializer
8. **开箱死锁**（admin）：`guide.seed.enabled` 默认 false 而科室/语料唯一来源就是它，全新环境挂号页无科室、导诊报「知识库暂无可用科室内容」；默认改 true（幂等）
9. **结论 JSON 不可用时丢回复**（rag）：无分隔符分支兜底写死空串，模型带花括号的追问会被清成空白气泡
10. **embedding 分批死循环 / 下标越界 NPE**（llm）：批大小配 0 会无限发请求；返回漏条抛 NPE 无提示
11. **置信度缺失口径不一**（chat/前端）：Top3 显示 0% 而头部显示「—」；前端另写死 0.5 阈值与后端 sys_config 打架
12. **换账号残留上一位患者对话**（前端）：跳登录只清用户态不清对话 store
13. **挂号失败死胡同**（前端）：除加载失败外只剩一行红字，无返回入口
14. **已有库缺迁移脚本**（sql）：`has_result` 只在建表脚本里补，老库启动即报 Unknown column；补 `migration_20260919_*.sql` 与 README 升级说明
15. `ChatService` 在模型漏输出分隔符时补 WARN 留痕（JSON 会随 delta 流进气泡，便于后续调 Prompt）

**未改（记录为设计取舍/遗留）**：追问超限强制结论时兜底科室可能不相关（设计接受低置信度结论，且优先取检索片段所属科室）；模型漏分隔符时 JSON 仍会流入气泡（已加留痕，靠 Prompt 迭代收敛）；精排降级时 Prompt 里的「相关度」是 RRF 分（有 WARN 可查）。

### 2026-09-19 · 链路 A 全链路打通（LLM 适配层 + RAG + 对话编排 + 埋点前半）

**完成**：
1. **common 基础设施**：pgvector 读写工具（余弦召回、锚点写、方向预筛）、ES chunk 工具（IK 分词 mapping、BM25、可用性降级）、错误码 5xxx/6xxx/7xxx 补齐
2. **llm 适配层**：DeepSeek 流式/非流式对话、DashScope embedding（按 index 回填 + 分批 + 维度校验）、阿里 rerank（模型名走配置，当前 `llm.dashscope.rerank-model` 默认 qwen3-rerank）；JDK HttpClient 手写 SSE 解析，不引新依赖；Key 缺失报 7000 不发请求
3. **rag 检索层**：查询改写（多轮时）→ 双路召回 → RRF(k=60) → 精排 Top-N → Prompt 拼装 → 结论解析；ES/精排/改写三级降级不中断导诊；`streamAnswer` 是 chat 唯一的生成入口
4. **chat 编排**：会话状态机（双信号新主诉判定）、SSE 七事件（session·delta·question·result·notice·done·error）、分流闸门（结论 JSON 不进气泡）、追问双路径、规则硬门槛、敏感词前置（禁止/观察/白名单防误杀）、结论落库（rec_top3 + evidence 快照）
5. **kb**：ChunkIndexService（MySQL→pgvector→ES 三写）、DeptService、MedicalTermService（唯一生效源 + 失效重建）、种子语料（7 科室 12 切片 72 术语）
6. **feedback 前半**：TrackService（三触点旁路）、SensitiveHitListener（事件解耦落 filter_log）、FilterAction.fromCode
7. **挂号确认**：actual_dept + top1_hit/top3_hit 同事务写入，会话置 closed，后端补记 register_success
8. **前端**：Chat.vue 接真实 SSE、SimRegister.vue 接科室列表与挂号、fetch 流式 SSE 客户端、chat store 扩字段

**验证**（真实模型 + 真实中间件，逐路径实测）：
- 正常结论：`爬楼梯胸口发闷伴左肩酸` → 心血管内科 86%（呼吸 8% / 消化 4%），note 与溯源引用合理
- 多轮追问：`肚子隐隐不舒服` → 模型生成针对性追问（部位/与进食关系/时长/伴随）→ 补充后出消化内科 78%
- 规则硬门槛：`在吗` → 模板追问，未调模型
- 敏感词：`你们就是骗钱，滚` → 拦截固定话术；`我要投诉你们医院` → 放行 + filter_log(watched) + 命中次数 +1
- 新主诉判定：已出结论的会话再发消息 → 自动开新会话（sessionId 变更）
- 停用科室：停用皮肤科后 `身上起红疹` → 皮肤科被过滤，顶上的呼吸内科 33% < 0.5 → lowConfidence=true（盲区榜分流）
- 挂号：actual_dept/top1_hit=1/top3_hit=1 落库，会话 closed，register_success 埋点后端补记；挂已停用科室被拦（5002）

**困难点与解法**：
- **pgvector 的 SQL 跨库 JOIN 了 MySQL 的 kb_chunk**（`relation "kb_chunk" does not exist`）——两个库不能互查。改为 rag 定义 `ChunkTextProvider` 端口、kb 实现（依赖倒置），向量路只回 id，融合后统一按 MySQL 回填正文，顺带丢弃脏向量
- **自定义 DataSource Bean 会让 MySQL 主数据源自动配置整体退让**（`@ConditionalOnMissingBean(DataSource.class)`），MyBatis 误连向量库。改为只暴露 `JdbcTemplate`（内部持连接池），不注册 DataSource 类型 Bean
- **HikariDataSource(HikariConfig) 构造即建连**，向量库不可用会拖垮启动 → 改无参构造 + setter，建连推迟到首次使用
- **chat 直连 LLM**（隐式走 rag 传递依赖）违反依赖规范 → rag 暴露 `streamAnswer` 与 `RagTurn`，chat 不再引用 llm 包
- `guide_vec` 库与 pgvector 表此前从未初始化 → 用一次性 JDBC 脚本执行 `pgvector_init.sql`（无 psql 客户端）
- ES 集群带 IK 分词器（analysis-ik 8.5.2）→ mapping 用 ik_max_word/ik_smart，中文 BM25 才有意义
- PowerShell/Bash 下 curl 传中文 JSON 会编码错乱 → 请求体一律落盘（UTF-8）+ `-d @file`

**遗留/待办**：
- 链路 B 上传流水线（MinIO 上传 → 解析切分 → 异步任务表 → 双写）未动工，当前靠 `guide.seed.enabled` 种子语料
- 链路 C 的聚合归桶、审核回流、看板未动工
- 停用科室场景下"全被拦截"的兜底逻辑只覆盖到低置信度分流，未做人工提示
- `filter_log`/`track_event` 目前无管理端查看入口

### 2026-09-19 · 管理端 B3 配色定案 + 侧栏/输入框打磨 + 方案稿整理（约 0.4 天）

**完成**：
1. 色板 B3 定案：主色 #3B82C4 向靛收敛至 #4468B8（blue-deep #33518F），侧栏三级靛阶（blue-side #3D5CA4 新增），wash 重调 #EDF1F9；新增语义浅底 coral-soft/moss-soft 与 EP 状态梯度（dis-bg/dis-text/mask）。同步 element-override.css（light-3~8 同调插值 + placeholder/disabled/mask）、admin.css 令牌、Dashboard.vue 硬编码色值。
2. 交互打磨：EP 按钮与输入框统一 32px 高（link 型除外）；输入框 hover/focus 只变色不变粗（hover blue-side / focus blue）；EP 组件内部原生元素不叠 `:focus-visible` outline（修双框）；账号搜索框加图标、清空自动重查。
3. 侧栏改版（AdminLayout.vue）：管理员移入侧栏底部（头像 + 昵称 + EP 下拉，退出登录带 confirm：清 Redis → 跳登录页 → toast）；侧栏可折叠 212px ↔ 60px（两字缩写 + title 提示）；顶栏移除用户名与退出按钮。
4. 方案稿整理：删除过时稿 `管理B-冷靛控制台.html`、`管理B2-知识库与映射页.html`（旧色板）；B3 稿删对照区、补侧栏用户区/折叠钮后转正为 `管理端方案/管理B3-冷靛控制台.html`；`前端设计方案.md` 同步（参考稿路径、B3 版本说明、顶栏/侧栏/表格/表单控件 §3.9）。admin.css 旧色 #FAFBFE 收编为 wash。

**困难点**：B3 稿「对照区」与旧稿并存会造成文档多处漂移——定稿后必须删稿并收敛到单一参考稿；模板误写 `< Fold`（`<` 后带空格）被 Vue 当文本节点渲染成裸标签串，浏览器截图核查才发现，组件标签书写需紧贴。

**验证**：vite build 通过；Edge headless 实测用户管理页展开/折叠/下拉三态截图正常。

### 2026-09-19 · 链路 D 真实联调 + Edge 浏览器 UI 实测（约 0.3 天）

**完成**（提交 898416f）：
- 联调环境跑通：后端 8080（MySQL/Redis @192.168.193.131）+ 前端 vite 5174
- 接口联调全过：登录签发 token、用户分页（4 账号）、封禁/解封、敏感词添加/批量导入（\n 换行拆分、去重、重复返回 2005）
- **UI 实测（Edge 无头，playwright-core 脚本 frontend/e2e_admin*.cjs）**：登录→用户管理→封禁确认弹窗→已封禁→解封→恢复正常，全链路通过；console 零错误
- 修复两缺陷：EP 中文 locale（Total 4 → 共 4 条）；favicon 404

**困难点与解法**：
- **8080 被旧实例占用**：登录成功但 /api/admin/users 报 1000——旧代码 404 被全局兜底成 1000，极具迷惑性；杀旧进程重启即好。教训：联调前先确认端口上是新代码（看启动日志 Tomcat started 时间）
- **Tomcat 绑到 60242**：显式 `-Dspring-boot.run.arguments=--server.port=8080` 强制（60242 为 WorkBuddy 占用，来源未深究）
- **agent-browser 不可用**（daemon 起不稳 + PowerShell stderr 管道炸断命令链）→ 改用 playwright-core + 系统 Edge 脚本（frontend/e2e_admin*.cjs，playwright-core 为 --no-save 本地安装，不入库）；此路稳定可复用，后续页面联调沿用
- PowerShell 管道遇 native stderr 会中断命令链：输出一律落盘读

**遗留**：5173 端口有用户自己的旧 vite dev 在跑（旧代码），联调请用 5174 或让用户重启；"近 7 天"按钮仍是假按钮（Dashboard 范围）

### 2026-09-19 · 管理端接入 Element Plus（约 0.2 天，用户点名提效）

**完成**（提交 c4b247b）：
- main.js 全量注册 EP；element-override.css 把 EP 变量映射到冷靛色板（primary=blue/success=moss/warning=coral、直角、字体），挂 :root
- UserManage.vue 全面换 EP 组件：el-table/el-tag/el-pagination（始终显示 total）/el-select/el-dialog/ElMessage/ElMessageBox 确认弹窗
- 规范记入前端设计方案.md：此后管理端新页面优先 EP 组件，不再手写表格/分页/下拉

**困难点与解法**：
- EP 弹层（dialog/select 下拉/Message）teleport 到 body，主题变量挂 .admin-root 覆盖不到弹层 → 改挂 :root（患者端不用 EP 组件，无污染）

### 2026-09-19 · 管理端用户管理（链路 D 收尾，约 0.5 天）

**完成**（提交 20f9cf7）：
- 后端：UserAdminService（用户分页+关键词搜索、封禁/解封、不能封禁自己）、SensitiveWordAdminService（分页/添加/批量导入去重/启停/转禁止词/删除）、两个 admin 控制器、ErrorCode 2004-2007、单测 +12（全模块 21 过）
- 前端：api/admin.js、Pager 分页器、UserManage.vue 假数据全部替换（搜索/封禁解封/行内添加/导入/启停/转禁止词/删除/txt 导出备份）
- .gitignore 排除 .workbuddy/

**困难点与解法**：
- `SensitiveWordType.valueOf("watch")` 抛异常——valueOf 匹配常量名（大写），入参是小写编码值 → 改为遍历 values() 按 @EnumValue code 匹配。**教训：所有"前端传枚举编码值"的反序列化都不能用 valueOf**（chat/kb 等模块后续要注意）
- 管理端无 Element Plus 注册（main.js 未 use），交互先例是自制 a-toast + 行内面板 → 沿用，新增 Pager/a-input/a-inline-panel 通用部件

### 2026-09-19 · 链路 D 前端接通 + 前端体验（约 0.5 天）

**完成**：
- 修复 `SecurityConfig` 403 误写 401（无权限时前端会误跳登录页）
- 新增 `api/auth.js`；Login.vue 假数据登录替换为真实接口，加登录/注册切换（注册默认患者角色、注册即登录）
- `http.js` 统一解包 Result：resolve 直接给 data；业务码 2000 清登录态跳登录；`code!==200` reject 带 message
- AdminLayout 与患者端 Chat 顶栏退出登录（先调后端删 Redis 登录态，再清本地态）
- 补 axios 依赖声明；患者端桌面端响应式（≥768px 收窄 720px 居中病历单列）
- 文档同步：总体架构 v1.5、前端设计方案（退出按钮/桌面适配规范）、CLAUDE.md 目录树
- 验证：`mvn test` 全模块通过；`vite build` 通过；提交 6 个（c151c9a…b153844）

**困难点与解法**：
- `http.js` 在用 axios 但 package.json 未声明 → vite build 报 Rollup resolve 失败 → 补依赖（写新依赖后必须跑一次 build 验证）
- http 拦截器只清 localStorage 不清 Pinia store → 路由守卫仍判定已登录会抖动 → 改为调 `useUserStore().logout()`
- PowerShell 中文输出乱码/不可见 → 统一落盘（UTF-8）+ Read 读取；git 中文 message 用临时文件 `git commit -F`

### 2026-09-18 及之前 · 链路 D 后端（时长未记录）

**完成**：JwtUtil（含 jti）/JwtAuthFilter（Redis 登录态校验）/SecurityConfig/AuthService（封禁校验、多端登录）/AuthController、JwtUtilTest+AuthServiceTest、mysql_init.sql（user 表+管理员账号+sys_config）、application.yml jwt 占位符、common 公共件、全部模块实体/枚举/mapper 骨架、管理端页面壳+患者端假数据页。

## 已知坑（施工前过一遍）

- **枚举反序列化禁止 valueOf**：前端/请求传的是小写编码值（如 "watch"），`valueOf` 匹配大写常量名必炸；统一按 code 遍历匹配（参考 SensitiveWordAdminService.parseType、FilterAction/TrackStage.fromCode）
- `kb_doc.status`(DocStatus) 与 `ingest_task.status`(IngestStatus) 是两套枚举，勿合并（见数据库设计 §0）
- 双写一致性顺序：先删 ES → 再删向量 → 再删元数据 → 再删 MinIO
- 前端 `views/common/Login.vue` 沿用患者端临床纸感样式，改样式时两处（页面 scoped + patient.css）都要看
- Windows 环境下 Bash 工具不可用时会退化为 PowerShell 语义；写文件/请求体一律显式 UTF-8，命令输出落盘再读（curl 内联中文 JSON 会乱码）
- **跨库不能 JOIN**：pgvector 存向量 + `doc_id` + 一份**切片正文副本**（只作人工排查用，检索不读）；正文事实源在 MySQL，向量召回拿 id 后经 `ChunkTextProvider` 回填（rag 定义端口、kb 实现）。**MySQL 为准、副本不作数**，故 `kb_chunk` 只写不改——任何引入 UPDATE 路径的改动都要同步三处（MySQL / ES / pgvector）
- **写 pgvector 与 ES 都不在 MySQL 事务里**：`indexChunks` 的 `@Transactional` 只管 MySQL。任一路写失败 → MySQL 回滚、向量留下（孤儿向量），必须靠 `ChunkIndexService.compensateWrittenVectors` 主动回删；读时兜底是 `RagService.fillTexts`（按 MySQL 存在性丢弃脏命中）。**别信"同一事务边界"那种说法**——它曾经写在文档里好几个月
- **别在容器里注册额外的 DataSource Bean**：会让 Spring Boot 主数据源自动配置退让，MyBatis 误连向量库；pgvector 只暴露 JdbcTemplate
- **HikariDataSource(HikariConfig) 构造即建连**；要延迟建连用无参构造 + setter
- **chat 不得引用 com.guide.llm**：生成走 `RagService.streamAnswer`，历史消息用 rag 的 `RagTurn`
- 后端启动依赖多模块 jar：改代码后先 `mvn -o install -DskipTests`，否则 `spring-boot:run` 会用到本地仓库里的旧 jar（表现为 NoClassDefFoundError）
- 端口 8080 被旧实例占用时，新实例起不来但 curl 仍打到旧代码上；先 `netstat -ano | grep 8080` 找 PID 再 taskkill
- **根因清单：存小写 key、显示中文**，唯一定义源是 `feedback/enums/RootCauseKey`（key 由常量名派生，防漂移；不参与 MyBatis 装载，故无 `@EnumValue`、也不要登记进 `EnumConventionTest`）。`root_cause.causes` 里是小写 key，页面显示 label；`patient_wrong` 是**唯一带后端语义**的一项——看板准确率口径靠它排除记录，且 `DashboardService` 从枚举取 key 而不是写字面量。**不要在别处再写一份清单**：审核页 `Review.vue` 目前镜像了一份本地清单（页面壳），接真实接口时改为从后端取
- **SSE 接口的 `produces=text/event-stream` 会吞掉错误响应**：客户端 Accept 只声明事件流时，异常处理器写 JSON 会协商失败（406/400 空体）；前端要同时声明 `application/json` 并按 content-type 分流
- **`GlobalExceptionHandler` 的 Result 只对非 SSE 接口可靠**：SSE 接口的错误要走事件流内的 `error` 事件，不要指望 HTTP 体
- 缓存类组件（术语白名单、sys_config、ES 索引）改「即时生效」语义时，必须同时给出 TTL 或刷新调用点，否则文档承诺与实现背离（本批已加 60s TTL 兜底）
- **纯单测里 `LambdaUpdateWrapper.set(SFunction, …)` 必炸**（`can not find lambda cache for this entity`），而 `LambdaQueryWrapper.eq(...)` 不炸：`Update.set` **调用当刻就解析列名**，`eq` 这类条件的列名延迟到生成 SQL 时才解析；纯单测（mock mapper、不起 Spring）没有注册 TableInfo。解法：`@BeforeAll` 里 `MpTableInfoTestSupport.init(Entity.class)`（见 `auth/src/test/.../MpTableInfoTestSupport`），别为它拉整个 Spring 上下文
- **不要把一个"提示"塞进 `delta` 事件**：delta 的语义是同一气泡的增量，提示会和紧随其后的答案粘成一段话、前端再也拆不开。要单独成泡就用独立事件（`notice`，见架构文档链路 A 对齐点）
- 阈值类配置**必须做跨项校验**：禁言阈值 ≤ 警告阈值时不会报任何错，但判定"先看禁言线"，警告档**永远不会触发**（页面正常、数字静默不对）。同类：任何"页面上两个输入框之间有关系"的参数都要在保存时拦。本轮新增一例：切分三参数必须**层层不降**（目标 ≤ 上限 ≤ 模型切触发），配反了同样是"某一档永远不触发"
- **Tika 指定编码要设 `Metadata.CONTENT_TYPE` 里的 charset，不是 `Metadata.CONTENT_ENCODING`**：`AutoDetectReader.detect` 先试 SPI 探测器（只引 `tika-parser-html-module` 时一个都不在 classpath），再读 `Content-Type` 的 charset 参数，**根本不看 `CONTENT_ENCODING`；不设就直接抛「Failed to detect the character encoding of a document」
- **`BizException(ErrorCode, detail)` 的消息是"错误码文案: detail"**（"文档不存在: 文档已删除"）。要给人看的自定义原因用 `(int code, String message)` 那个构造，否则拼接串会直接成为管理端看到的失败原因
- **Git Bash 里 `curl`/`python`/`javac` 都是原生 Windows 程序**：路径要写 `C:/...`，`/c/...` 它们不认（bash 自己的命令如 `mkdir`/`ls` 则相反）
- **轮询类任务要"先推进状态再投递下一段"**：解析完成后若不立即改阶段，下一轮扫描会再挑中它、再投一次 → 内容写两遍。阶段字段就是"已投递"的判据（`IngestTaskService.moveToSplit` 幂等）
- 入库扫描**顺带要判"卡住的任务"**：进程中途退出会让任务永远停在 `running`，而「同一文档同时最多一个 running 任务」又会挡住重跑——不判中断就是个死胡同
- **DocumentMind 的 `index` 是页内序号，排序键是 `(pageNum, index)`**：第 2 页的 index 从 0 重来。只按 index 排会把两页**逐条交错**，而每块文本都完整——看单块看不出问题，症状只是"召回的内容前后不搭"。同类坑：**任何"服务端已排好序"的假设都要用真数据验一次**，别写"防御性地再排一次"
- **MinIO SDK 的 `contentType` 不能传 null**（`content type must not be null.`）：multipart 的 part 可以不带 Content-Type，透传就炸成 1000 系统内部错误。`MinioUtil.upload` 里回落 `application/octet-stream`
- **单字敏感词 + `contains` 子串匹配 = 误杀合法长词**：种子里有「滚」，于是「回滚」「打滚」全被入口拦下（本轮 4 道题的检索直接没跑）。词表白名单只救"该词本身是医学术语"的情形。新增单字词前先想一遍它嵌在常用词里会不会误伤
- **登录后的落点由角色决定，`redirect` 不能无条件优先**：`redirect` 是**未登录访客的入口路径**生成的，而访客在登录前不知道自己是什么角色。让 redirect 压过角色 → 从站点根进来的管理员被送进患者端（2026-09-26 实测过）。规则：管理员只认 `/admin` 开头的 redirect，患者只认非 `/admin` 的，其余回各自的默认首页

## 下一步（按依赖顺序）

### 先联调：四块已验证但没跑过真实环境

这四块目前只有编译 + 单测通过，**都没有真实环境联调**：

0. ~~**向量表迁移（阻塞项，先做）**~~ **✅ 2026-09-26 完成**：step1（加列，此前已做）→ 回填 12 条 → step2（收紧 `NOT NULL`），`doc_id IS NULL` 已为 0

1. **规则门槛（链路 A）**：无效输入（「嗯」「在吗」）在第 2 轮也被拦；有效短回答（「三天了」「左边，按压更疼」）不被拦；追问预算用尽（`ask_max_rounds`）后规则让路
2. **LLM 配置（管理端）**：页面加载（模型信息 + 参数）、改参数保存后导诊立即用新值、连通性探测三态（全通 / 部分失败 / 密钥未配）
3. **数据看板（管理端）**：KPI / 趋势 / 根因 / 记录加载、三种空态、分页翻页
4. **敏感词累计处置**：**先执行 `scripts/sql/migration_20260920_user_violation.sql`**（旧库不执行会因 `user.mute_until` 缺失导致每轮对话直接失败）。验证点：反复发同一句含禁止词的话看计数是否累加、把阈值临时调到 3/5 跑通「警告 → 禁言」，观察词（阈值调到 2）只警告不禁言、禁言期内发消息返回禁言话术且**不再累计**、禁言到期（把 `sensitive.mute.minutes` 调到 1）后自动恢复、用户管理页的触发次数与明细、保存"禁言阈值 ≤ 警告阈值"被拒

联调环境同上批：后端 8080（必须显式 `--server.port=8080`）+ 前端 5174（5173 有用户自己的旧实例在跑旧代码）。

### 按依赖顺序开工（2026-09-20 用户决定「先不做、先更新文档」，故只登记不动工）

- **审核队列**（依赖链路 C 后半，建议拆三步——这是施工计划，架构文档 §7 只记设计口径，不记顺序）
  1. **聚合归桶**：精确归桶 + 语义归桶（pgvector 锚点向量 + 方向预筛）+ 分类型阈值升级 → 产出 `cluster_bucket` / `review_task`（**现均为空表**。原料是有的：`guide_record` 已有 20+ 条含反馈的记录；这一步是审核页做不下去的真正原因，页面本身 2026-09-26 已清成诚实空态）
  2. **审核页读与审**：待审列表、证据链回放、根因归因（写 `root_cause` + `root_cause_log`）、驳回 / 忽略 / 修正重审；根因选项从后端 `RootCauseKey` 取（页面里那份镜像清单已随假数据删除，重开时别再抄一份）
  3. **approve 产出**：映射台账（键 = 症状原文 + 主科室）+ 合成 chunk 预览可编辑定稿
  **依赖已解除**（2026-09-26）：第 3 步的 chunk 入库依赖**链路 B 上传流水线**，该流水线后端已完成——合成 chunk 现在有了落地的通道（挂主科室的「回流补充 · XX科」容器，走同一条切分/入库路径）。**仍不要为审核回流另开 kb 直写通道**：那会破坏「kb 是唯一写向量库与 ES 入口」这条硬约束
- **知识库管理**（链路 B）：~~后端流水线~~ ~~管理端页面~~ **✅ 2026-09-26 全部完成**（后端全量 + 页面四个 tab 接通 + 真跑验证）。剩下的：
  - ~~**管理端知识库页接真实接口**~~ **✅ 2026-09-26 完成**（四个 tab 全接 + 后端补 5 个维护接口，见上条施工日志）
  - **实测暴露的上游损耗要不要处理**：图片内文字无块标记（混进正文）、OCR 字符混淆（`、`→`丶`、`P0`→`PO`）、跨页表格拆两块——三条都是"要不要做检索前归一"的问题，先定做在入库侧还是查询侧（见上条施工日志待办）
  - **可以后做**：术语抽取（LLM 批量实体抽取 → ES terms 聚合 → 白名单刷新）走**旁路**，不做也能跑通，白名单暂时靠种子 + 人工维护；**白名单的人工新增入口也要等它**（现在页面上只能启停已有的 72 条）

### 可顺手清的小遗留

- **`track_event`（三触点埋点）仍没有管理端查看入口**；`filter_log` 现在能看了——用户管理页行的「明细」弹窗就是按用户看的 `filter_log` 聚合（窗口内按词分组的命中 + 处置记录）
- 停用科室「全被推荐拦掉」时只走了低置信度分流，没给患者任何人工提示
- 敏感词那块上一轮审查发现但**未改**的三条：删词后重加同名词会撞物理唯一键（逻辑删 + `@TableLogic` 导致 `exists()` 查不到、但 `uk_sw_word` 还在）；`contains` 大小写敏感且不处理变形词（全角/插符号/拼音绕过）；每轮对话全表扫词库 + 逐词 `contains`——后两条现在更值得做了：**累计处置会放大误判**（误命中一次就是一次违规计数），而且命中路径上多了一次窗口计数查询
- 累计处置的判定是**同步查一次 `filter_log` 计数**（每轮命中都查，未加缓存）；命中量上来后可考虑短 TTL 缓存，但要先想清楚"缓存陈旧导致漏处置"是否可接受
- 警告/禁言只在**对话内**提示（`notice` 气泡），患者不发起对话就看不到；将来若要触达需要额外通道（站内通知/短信），当前不做
- 术语白名单口语缺口：没有「疼 / 肚子 / 发烧 / 拉肚子 / 嗓子」等口语别名，是将来要在应答轮开内容门槛的前置条件
