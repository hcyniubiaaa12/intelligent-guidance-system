# 智能导诊系统

患者输入主诉，系统经 RAG 检索推荐科室，患者模拟挂号确认，反馈闭环驱动知识库修正。本文件是领域词汇表：术语冲突以此为准。

## Language

### 会话与导诊

**会话（Session / chat_session）**:
一条主诉的完整生命周期：从第一条症状描述开始，到挂号成功关闭（或出结论后患者另起新主诉）结束。一个聊天页面可先后承载多个会话。
_Avoid_: 对话（对话是聊天页面的视觉概念，一个页面多次对话≠多次会话）

**导诊记录（Guide Record / guide_record）**:
一条主诉一份"单据"快照：推荐结论、Top3 候选、证据、实际挂号结果。会话与导诊记录严格一对一。
_Avoid_: 诊断记录（系统不做诊断，只做分诊推荐）

**主诉（Chief Complaint）**:
患者对一个不适症状的自然语言描述，是会话的起点与归属边界。
_Avoid_: 问题、诉求

**追问（Question）**:
信息不足时系统发出的针对性补充提问，最多 3 轮；只补信息，不重复出结论。

**用户问题**:
一次会话里**患者说的每一句**（`chat_message.role=user`），按时间从 1 编号——它就是「就诊记录」页右缘书签的锚点。
_Avoid_: 与「追问」混用（追问是**系统**发的补充提问，方向相反；判据是 role：user 才是用户问题，question 不是）

**就诊记录（我的就诊）**:
患者查看自己既往就诊的地方——一个聊天页先后承载的多个会话（= 多次就诊）按天铺开可回看。
**就在首页左侧栏**：点会话即在对话区只读回放，点昵称切到「挂号历史」。没有单独的记录页。
_Avoid_: 病历（系统没有病历，只有会话与导诊结论）

**回放（replay）**:
把某次会话的完整过程在对话区按原顺序只读重现（用户气泡 / 追问 / 结论卡）。
**只读**——一次会话 = 一次就诊，会话结束了就不能再往里写字，要继续问就是新会话。
_Avoid_: 加载历史（含糊，且暗示还能接着往下写）

**新主诉判定**:
会话已出导诊结论（result）或状态为 closed 后的下一句输入 = 新会话。判定信号是状态机字段（has_result / status），不是模型语义判断；未出结论的进行中会话内，输入按追问处理。

### 盲区与错误（两类信号，性质相反）

**盲区榜**:
低置信度记录（low_confidence=1，top1 置信度 < 0.5）的清单——系统**自知不确定**的主诉。不进准确率统计、不进聚合。
_Avoid_: 知识盲区（裸用）、盲区分流

**错误模式桶（聚合桶 / cluster_bucket）**:
top3 未命中记录按（推荐科室, 实际科室）方向聚类的桶——系统**自信但错了**的样本聚类。进准确率统计（算 miss）并升级待审。
_Avoid_: 知识盲区信号、系统盲区信号

**排序精度问题**:
top1 未命中但 top3 命中——患者本就在推荐列表里选的，不算系统错，不进回流。

### 回流

**回流（approve 的完整产出）**:
管理员 approve 聚合桶时系统的写回动作 = 写映射台账 + 生成合成 chunk 走链路 B 流水线。回流只前向修正，永不回改历史导诊记录。
_Avoid_: 回写、回改

**合成 chunk**:
由审核结论生成的鉴别语料（症状→正确科室），挂**主科室**的系统级「回流补充 · XX科」容器（每个科室一个，所以切片继承的科室天然就是这条语料讲的科室），走与文档上传同一条入库流水线。闭环生效的唯一机制：RAG 检索到它，同类主诉推荐变对。
_Avoid_: 知识片段刷新（含义模糊，不指明产出物）

**映射台账（dept_mapping）**:
症状→科室映射的事实记录，供管理端维护与审计；运行时**不被 RAG 消费**——闭环生效靠合成 chunk，不靠映射。

**主科室（main dept）**:
映射行里的首诊科室——该症状首先该挂的科。审核 approve 时由管理员与交叉科室一同给出。
_Avoid_: 目标科室（旧称，同一概念，已废弃）

**交叉科室（cross dept）**:
该症状还需一并考虑、需要鉴别的其他科室——回答"还可能是什么"，不是"系统当时误判成了什么"。审核 approve 时由管理员与主科室一同给出。
_Avoid_: 混淆科室（那是"易误判对象"的错误统计视角）、备选科室（暗示与主科室同权重，实际是次要鉴别对象）

**重新入库（reprocess）**:
管理端对已有文档手动重走的入库流水线；与回流共用链路 B，触发源不同。
两条硬语义：① **用 MinIO 里的原文件**——没有"替换文件"这个操作，要换内容就删掉这份文档、重新上传；② **先删旧切片再重建**（不是先写新再删旧），所以**失败时这份文档的内容会缺席**，直到人工再点一次——这是明知的取舍，不要顺手加"失败回滚到旧切片"。

### 知识库入库

**文件上传（upload）**:
把原始文件送进 MinIO 的动作。**同步、无痕**——失败只有错误响应，不产生 `kb_doc`。
_Avoid_: 与「文档入库」混用（管理端那个「上传文档」按钮，实际触发的是**文档入库**，不是这个）

**文档入库（ingest）**:
建 `kb_doc` → 解析 → 切分 → 写库的完整过程。**异步、留痕**——失败会留下一条 `status=failed` 的记录，管理端列表看得见、可重新处理。
_Avoid_: 只说「上传」（"上传失败怎么办"这句话恰恰因为不区分这两件事而永远含混：说的是文件没传上去，还是文档解析失败？两者的表现完全不同）

**解析（parse）**:
把上传的原始文件变成**文本块**（每块带类型与坐标、**无层级**）的过程。格式分流：`pdf`/`docx`/`png` 经 `llm` 适配层调 DocumentMind（**唯一路径，不降级**），`html` 走 Tika，`txt`/`md` 原生读。
_Avoid_: 与「切分」混用（解析产出文本块，切分把文本块变成 chunk；解析产物没有层级，且文本块必须按坐标排序才有正确阅读顺序）

**切分（split）**:
把文本块变成 chunk 的过程，三层自上而下——**结构切**（标题块 = 边界，`title` 取最近的标题块）→ **递归切**（无条件兜底，超长块必走）→ **模型切**（只在没有标题块的部分出场，只给边界、不给 title）。参数走 `sys_config`。
_Avoid_: 说成"三层可选"（层是固定的、自上而下依次生效，可配的只有参数；少了递归切兜底就会出现"无策略可走"的输入）

**产出校验**:
模型切之后对切分结果的检查：拼接后是否覆盖原文（丢字检测）、块数与原文字数是否匹配、每块字数是否落在区间内。校验不过 → 文档标 `failed`，不入库。
_Avoid_: 依赖"模型切不出来会返回空"（模型不会说自己切不出来，它会编一个看起来合理的切分）

**版面块（layout）**:
解析服务返回的文档结构单元，每块带**类型**（`title` / `text`）、字号、页号、阅读顺序号与 markdown 形态；表格块另有 `numCol` + 单元格网格坐标。
_Avoid_: 与「切片（chunk）」混用（版面块的粒度由**解析器**定、是解析的产物；chunk 的粒度由**切分参数**定、是切分的产物。切分层的输入是版面块，输出是 chunk）

**入库任务（ingest_task）**:
一次文档处理运行的记录——阶段（解析/切分/向量化/入库）、进度、失败原因、异步句柄，以及外部解析服务的任务号（`external_job_id`）。一个文档 N 次运行（首次 + 历次重新入库）就有 N 行。
_Avoid_: 与「文档状态」混用（`kb_doc.status` 是文档的**当前态**、一个文档一行；任务表是**每次运行**的记录。列表展示读文档，重试与排查读任务）

**切片正文副本**:
pgvector `kb_chunk_vec` 里的 `title` / `content` 两列，是 MySQL `kb_chunk` 正文的副本，**只作人工排查用**。检索路径不读它，事实仍以 MySQL 为准。
_Avoid_: 说成"向量库也存正文所以不必回填"（回填照旧走 MySQL，副本不作数）

**孤儿向量**:
MySQL 侧已回滚、但 pgvector 里已落库的向量——成因是 pgvector 与 ES 都不在 MySQL 事务里。读时由 `ChunkTextProvider` 回填丢弃（不进 Prompt），写时由写时补偿回删。
_Avoid_: 与「脏命中」混用（脏命中是读时看到的现象：向量在库、元数据已删；孤儿向量是写时留下的成因）

**写时补偿**:
chunk 写入过程中任一路（向量 / ES）失败时，用本次内存里记下的 chunk_id 批量回删已写入的向量，再向上抛出。与「删除补偿」（删文档时的 ES → 向量 → 元数据 → MinIO 顺序）是两件事。
_Avoid_: 说成"回滚"（MySQL 有事务能回滚，pgvector 与 ES 没有）

### 审核与归因（两个不同的管理动作）

**审核（review / review_task）**:
对错误模式桶给出主科室与交叉科室并 approve 的动作——产物是映射台账 + 合成 chunk（改知识库）。
_Avoid_: 审批（与审批流混淆）

**驳回（rejected）**:
桶的"否"终态之一：经审核否定——人看过并判定"不是系统错"（如患者挂错、样本误聚合）。留下的是审核意见。
_Avoid_: 与"忽略"混用（忽略是没审就关）

**忽略（dismissed）**:
桶的"否"终态之一：没审就关——测试/演示数据、重复桶等噪声被清掉。留下的是清理记录，不构成审核意见。
_Avoid_: 与"驳回"混用（驳回是审过之后的否定）

**根因归因（root cause）**:
对单条全错记录勾选预定义根因（切分破碎/检索失败/患者挂错……）的动作——产物只进看板"错误根因分布"（诊断信号），不改知识库。标"患者挂错"的记录从看板 miss 口径排除，但归因与驳回是两个独立动作，都要做。

### 科室

**文档科室（kb_doc.dept_id）**:
一份知识文档**只归属一个科室**，它的全部切片**继承**这个科室——向量表里的 `dept_id` 就是它，chat 层按它做推荐校验（停用科室的切片留库可召回、但绝不入推荐）。跨科室的内容请**拆成多份文档**上传。
_Avoid_: 说成「切片的科室」（切片没有自己的科室，是继承来的）。现在两者是同一个值，但一旦讨论到多科室文档，这两个词必须分开说——否则"这个科室"指谁永远说不清

**科室停用（enabled=0）**:
仅入口生效：挂号页不显示 + 推荐校验拦截两道门；停用科室的 chunk 留库可被召回但绝不入推荐；历史记录引用不受影响。
_Avoid_: 科室删除（系统不存在物理删科室）

**推荐科室（rec_dept）**:
导诊记录中系统推荐的 top1 科室（结论主体）。
_Avoid_: 诊断结果（系统只分诊不诊断）

**实际科室（actual_dept）**:
患者在模拟挂号确认时选择的科室——准确率计算的唯一依据。患者可以不挂号（流失）：actual_dept 为空的记录不参与准确率统计，口径 = 已确认记录。

**结论（三形态，同一数据）**:
SSE `result` 事件（传输形态）→ 推荐卡（前端渲染形态）→ guide_record（落库形态）。三个词指向同一份数据：Top3 科室 + 置信度 + 溯源引用。

**溯源引用（注号）**:
推荐卡「判断依据」脚注里的条目，注N 对应证据快照 `retrieved` 的第 N 条知识片段——与 Prompt 中给模型的注号是同一套编号，用于回答"凭什么这么推荐"。
_Avoid_: 参考文献（不是外部文献，是本次检索命中的知识片段）
