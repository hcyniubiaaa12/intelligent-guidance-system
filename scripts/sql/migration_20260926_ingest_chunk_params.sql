-- ============================================================
-- 增量迁移：sys_config 新增 5 条入库参数（切分 3 条 + 轮询 2 条）
-- 背景：链路 B 上传流水线动工。切分参数**必须**可调，这不是可选项——
--       链路 C 根因表里 `chunk_broken`（切分破碎）的修复动作原文是
--       「对该文档单独重新入库（**调整切分参数**）」：参数写死在代码里，
--       这个修复动作就是空的，你能看出切得不好，但改不了、也重跑不了。
-- 影响：不执行时功能照常（键缺失会回落代码侧默认值），只是管理端改不了切分口径。
-- 执行：已在旧库上跑过 mysql_init.sql 的环境执行本脚本；全新初始化无需执行（脚本已含）。
--
-- 键的归属：
--   chunk.*        → 管理端「LLM 配置页 → 文档切分」面板可调（走 SysConfigAdminService 白名单）
--   ingest.*       → 运行时可调但**本轮不上管理端页面**（只在库里可改），
--                    轮询间隔与解析总超时属于运维参数，不是业务口径
-- ============================================================

INSERT INTO `sys_config` (`id`, `config_key`, `config_value`, `remark`) VALUES
('c13', 'chunk.target.length',           '400',  '切分：目标切片长度（字），递归切尽量往它靠'),
('c14', 'chunk.max.length',              '800',  '切分：单切片上限（字），超过它必走递归切'),
('c15', 'chunk.model.min.length',        '1200', '切分：模型切触发长度（字），无标题的连续文本达到它才调模型'),
('c16', 'ingest.poll.interval.seconds',  '10',   '入库：轮询外部解析进度的间隔（秒）'),
('c17', 'ingest.parse.timeout.minutes',  '30',   '入库：单次解析总超时（分钟），超了标 failed 可重试')
ON DUPLICATE KEY UPDATE `updated_at` = `updated_at`;
-- ON DUPLICATE KEY UPDATE 里刻意不覆盖 config_value：重复执行不冲掉管理端调过的值
