# 智能导诊系统 —— 总体架构与链路设计

> 版本：v1.12 · 2026-09-20（新增 §6.6 敏感词按用户累计处置：滑动窗口与阈值口径、禁言与封禁的分离、命中计数端口与 notice 事件）
> 定位：编码前必须对齐的架构与链路基线（表结构/接口明细搁置，以链路定案为准）。
> 阅读顺序：总体架构 → 四条核心链路 → 模块职责边界 → 已定参数汇总。

---

## 1. 总体架构

### 1.1 分层视图

```
┌─────────────────────────────────────────────────────────────────┐
│  表现层                                                          │
│  ┌────────────────────────┐  ┌──────────────────────────────┐   │
│  │ 患者端（Vant · 手机）    │  │ 管理端（Element Plus · PC）    │   │
│  │ 对话/推荐卡片/模拟挂号   │  │ 知识库/审核/看板/LLM 配置      │   │
│  └─────────────┬──────────┘  └───────────────┬──────────────┘   │
│                │HTTP+SSE (JWT)                │HTTP (JWT)        │
├────────────────┼──────────────────────────────┼─────────────────┤
│  业务层（Spring Boot 单体）                    │                 │
│  ┌─────────────┴──────────┐  ┌────────────────┴──────────────┐  │
│  │ 用户与鉴权              │  │ 问诊对话（SSE 编排）            │  │
│  │ Security+JWT 角色权限  │  │ 会话/多轮/追问/结论落库         │  │
│  └────────────────────────┘  └───────────────┬──────────────┘  │
│  ┌────────────────────────┐  ┌────────────────┴──────────────┐  │
│  │ 知识库管理              │  │ RAG 检索层（技术核心）          │  │
│  │ 科室/文档/映射维护      │  │ 改写→召回→重排→Prompt→解析      │  │
│  └────────────────────────┘  └───────────────────────────────┘  │
│  ┌────────────────────────┐  ┌──────────────────────────────┐   │
│  │ 反馈闭环                │  │ 统计看板                      │   │
│  │ 埋点/比对/聚合/审核回流  │  │ 准确率/分布/盲区              │   │
│  └────────────────────────┘  └──────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ LLM 适配层（唯一外部依赖出口，可插拔）                      │    │
│  │ DeepSeek 对话 · 阿里 embedding · 阿里 rerank             │    │
│  └─────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────┤
│  数据层                                                        │
│  ┌────────────────────────┐  ┌────────────────────────────┐    │
│  │ MySQL 业务库            │  │ PostgreSQL + pgvector 向量库 │    │
│  │ 用户/会话/导诊/反馈/    │  │ 知识片段 + embedding(HNSW)   │    │
│  │ 聚类/文档/映射/日志     │  │                            │    │
│  └────────────────────────┘  └────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────┤
│  离线侧（异步线程池 + 任务表 + 对象存储）                         │
│  ┌────────────────────────┐  ┌────────────────────────────┐    │
│  │ 异步线程池 + 任务表     │  │ MinIO                      │    │
│  │ 入库流水线阶段编排      │  │ 知识文档对象存储            │    │
│  │ （taskId 追踪/幂等/重试）│  │ （数据库只存链接）          │    │
│  └────────────────────────┘  └────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 架构设计原则（对齐基线）

1. **RAG 层与业务层解耦**：业务层（对话编排）只调用 RAG 服务接口，不直接触碰向量库与 LLM；RAG 层不感知业务状态（会话、用户）。
2. **LLM 唯一出口**：所有外部模型调用（对话/embedding/rerank）必须经过 LLM 适配层，业务代码不得直连第三方 API。
3. **反馈闭环非侵入**：埋点只"旁路采集"，绝不阻塞/修改导诊主流程；导诊失败不影响患者继续对话。
4. **双库职责单一**：MySQL 管"事实"（谁、何时、结果如何），pgvector 管"语义"（片段内容与相似度）；双库 + 对象存储的一致性由链路 B 的三层防线保证（详见链路 B 对齐点）。
5. **回流只前向修正**：反馈回流只新增/修订映射与知识片段，永不回改历史导诊记录（历史即事实，看板口径不变）。
6. **人工评审为质量闸门**：聚合负责粗召回候选（宁可混、不能漏——漏了问题永远不升级，混了只是多审一条）；审核负责精细判定；**写回知识库的唯一路径是人工 approve**，聚合歧义再大也到不了知识库。

---

## 2. 核心链路

### 链路 A：患者导诊对话链路（主链路）

```
患者端 Chat 页                        后端（对话编排 + RAG）              外部
───────┬───────────────────────────┬──────────────────────────┬────────
  ① 输入症状描述                     │                          │
  ② POST /api/chat/message 开启SSE   │                          │
       │ ─────────────────────────→ │                          │
       │                            │ ③ 会话/消息落库           │
       │                            │ ④ 入 RAG 编排            │
       │                            │    ├ 查询改写(多轮时)     │
       │                            │    ├ embedding           │ ──→ 阿里 embedding
       │                            │    ├ pgvector 召回 Top20  │
       │                            │    ├ ES 关键词召回 Top20  │ ──→ ES(BM25)
       │                            │    ├ RRF 融合去重          │
       │                            │    ├ rerank 重排 Top5     │ ──→ 阿里 rerank
       │                            │    └ 拼 Prompt            │
       │                            │ ⑤ 流式生成               │ ──→ DeepSeek
       │ ◄── SSE: delta 增量文本 ─── │    （逐字转发）           │
       │ 逐字渲染对话气泡             │                          │
       │ ◄── SSE: result 结构化结果 ─ │ ⑥ 解析 JSON→校验科室名   │
       │ 渲染推荐卡片(科室+置信度+    │ ⑦ 导诊记录落库            │
       │   溯源引用)                 │ ◄── SSE: done ─────────── │
  ⑧ result_view 埋点（自动上报）      │                          │
       │ ─────────────────────────→ │ ⑨ 埋点落库（旁路，不阻塞）│
```

**对齐点**：
- SSE 事件协议四态：`delta`（对话流）→ `result`（结论卡片）→ `done`（收尾）→ `error`（兜底）；外加 `session`（建流即发，回传本轮流所属会话 id）与 `question`（追问，见下）、`notice`（处置提示，见 6.6）
- **`notice` 必须独立成事件、不能塞进 `delta`**：`delta` 的语义是"同一个气泡的增量"，提示混进去会和紧随其后的答案粘成一段话，前端再也拆不开。前端收到 `notice` 时另起一泡，并把后续 `delta` 落到新气泡（见 `.claude/rules/前端设计方案.md`）
- **SSE 客户端形态（实现定案）**：前端用 `fetch` + `ReadableStream` 手写解析，**不用 EventSource**——EventSource 既不能带 `Authorization` 头也不支持 POST 请求体。事件名与载荷见 `chat/dto/SseEvents`，`streamAnswer` 的分流闸门保证结论 JSON 不进入气泡
- 追问双路径：模型先流式吐追问文本（`delta`）→ 收尾发 `question` 事件**定性**该气泡（不重复渲染）；规则硬门槛直接给模板追问时无 `delta`，`question` 事件负责渲染新气泡。前端据此区分"标记"与"新建"
- 埋点分工：`result_view` / `sim_register` 由前端上报 `/api/track`；**`register_success` 由挂号确认接口在后端补记**（事实与埋点同源，避免前端漏报）
- 置信度口径：**模型自报**——LLM 输出 JSON 里给出 Top3 各自的置信度，后端校验合法性（0–1 区间、单调递减），异常时置 null 并按低置信度分流处理（进盲区榜）。链路位置：精排之后拼 Prompt 时 **rerank 分数随 Top5 片段透传入上下文**，作为模型自报置信度的客观锚点（精排产出顺序与分数，置信度在生成阶段自报，不单独产生）
- **提示词外置**（prompts.yml，经 `spring.config.import` 引入）：导诊主提示词、查询改写、模板追问、拦截话术都在配置里，改提示词不改代码；模板占位符 `{knowledge}/{depts}/{marker}/{extra}/{dialogue}` 由代码填充，**结论分隔符是代码侧协议常量**（提示词用 `{marker}` 引用，避免两边不一致）；启动时校验必填项与占位符，配错直接启动失败
- **可观测性**：链路各阶段（入口校验/双路召回/RRF/精排/生成/解析/落库）打 INFO 日志并带轮次标记 `[sXXXXXX-rN]`（MDC `turn`），一行一个关键数据（条数、分数、置信度、耗时）；逐条召回明细与模型原始输出走 DEBUG
- 科室校验（⑥）：校验条件 = dept 表 enabled=1；Top3 混入停用科室则过滤、后续候选顶上；全被拦走低置信度分流（进盲区榜）。停用科室 chunk 留库可被召回但绝不入推荐，历史记录引用不受影响
- 一条主诉只产出一条导诊记录（追问轮只补信息，不重复出结论）；信息不足时先发 `question` 追问，不发 `result`
- 前端埋点时机 = 推荐卡片渲染完成瞬间（result_view）
- 后端返回 recordId 是后续埋点/挂号的唯一凭证

#### 信息充足性判定与追问机制（链路 A 配套）

**判定策略（混合）**：
- 第一层·规则硬门槛（两类，命中即模板追问，不调模型、不检索）：
  - **确定性无效输入**：语气词/寒暄/说不出/纯符号（如"嗯""在吗""不知道""。。。"）——**不区分轮次**。这类输入在任何上下文下都不含分诊信息，拦掉省一次模型调用。判定为"整条（去尾部标点后）精确等于无效词表，或整条不含任何文字与数字"，**不做子串匹配**（否则"没有发烧""不知道怎么说"会被误杀）；词表刻意不收"没有/是的/对/好的"这类否定与确认应答，它们在追问上下文里正是有效回答
  - **首条主诉过于笼统**：过短（< 6 字）且未命中部位/症状术语（如"我不舒服"）——只有自由陈述才要求它自带"部位 + 症状"
- **护栏·追问预算让路**：追问轮次达 `ask_max_rounds` 后规则门槛一律放行。规则门槛位于 forceConclusion 判定之前，无此护栏会让 `ask_round` 只涨而永远走不到检索分支、forceConclusion 永不触发，会话卡死在"回答 → 被问同一句 → 再回答"的循环里
- **应答轮不叠加内容门槛（明确取舍）**：追问的回答天然是短句且大量用口语（"三天了""左边，按压更疼""还拉肚子""没有发烧"），而术语白名单是书面医学词形（无"疼/肚子/发烧/拉肚子"这类别名），拿"未命中术语"或"字数下限"当应答轮闸门会系统性误杀有效回答。应答轮的语义判断交给持有完整历史的第二层；若后续要开这道闸门，前置条件是先补白名单口语别名
- 第二层·判定与生成合并为一次 LLM 调用：本来就要调一次模型——把"信息是否足够"的判定合并进同一次调用。模型输出双分支：信息足 → 走 recommend 出结论；信息不足 → 走 need_ask + 针对性追问（不发 result）。判定与追问零额外成本，质量是模型级的

**追问规则**：
- 针对性追问：结合检索到的知识片段，问"该症状鉴别最需要的信息"（部位/性质/诱因/时长/伴随），问题由模型自然生成，不是泛泛问"还有吗"
- 信息累积：每轮追问后的判定基于"历史对话 + 当前回复"的完整上下文
- 3 轮上限：3 轮后仍不足不强求——直接出结论但带低置信度，自动接盲区榜分流（不进准确率统计，避免污染口径）
- 轮次上限与检索 Top-K/Top-N 等参数运行时从 `sys_config` 读取（管理端可调），代码里只留默认值兜底

**检索数据流（实现定案）**：pgvector 只存向量与 chunk_id（管语义），切片标题与正文在 MySQL（管事实），**跨库不能 JOIN**——向量召回拿到 id 后，由 rag 层经 `ChunkTextProvider` 端口（rag 定义、kb 实现，依赖倒置）批量回填正文，顺带丢弃"向量在库、元数据已删"的脏命中。ES 路本身带正文，两路在 RRF 融合后统一以 MySQL 为准。

### 链路 B：知识库入库链路

```
管理端 DocManage 页        后端（异步线程池 + 任务表）          MinIO / pgvector
───────┬───────────────────┬─────────────────────────────┬──────────────────
  ① 上传文档                │                             │
  ② POST upload ─────────→ │ ③ 文件进 MinIO（存链接）       │→ MinIO
       │                   │    建任务表(taskId, 阶段=解析) │
       │                   │ ④ 提交线程池 → 解析→切分      │
       │                   │ ⑤ 切分 → embedding           │
       │                   │    → 批量写 chunk             │→ pgvector
       │                   │ ⑥ done/total 计数收敛 → 文档完成 │
  ⑦ 状态刷新(按 taskId) ◄─ │  （任务表是权威状态，幂等/重试    │
       │                   │   以 taskId 定位；线程池与 SSE   │
       │                   │   线程隔离，不拖慢在线导诊）       │
```

**组件作用**：
- **异步线程池 + 任务表**：入库流水线阶段编排（解析→切分→向量化→入库），阶段间异步衔接、批量上传排队；任务表记录进度（taskId 定位当前阶段），幂等与失败重试都以 taskId 为键；**只服务离线入库，不碰在线导诊链路（线程隔离）**
- **MinIO**：对象存储，存文档原始文件；数据库只存链接/路径；文档更新 = 新文件替换 + 路径更新；解析重试可重复拉取原文

**对齐点**：
- 文档状态机：`解析中 → 完成 / 失败`，失败按原因分类（可重试/不可重试），可手动 reprocess
- 一致性（双库 + ES + 对象存储）三层防线：
  ① 入库校验：切分入库前查文档/任务表是否仍有效（处理中被删则不写向量与 ES）
  ② 状态机收敛：全部 chunk 写入成功（pgvector + ES 双写完成）才置"完成"（done/total 计数），失败保持可重试
  ③ 删除补偿：先删 ES → 再删向量 → 再删元数据 → 再删 MinIO 文件，顺序保证 + 失败标记待重试
- **chunk 双写**：切分后 chunk 同步写 pgvector（向量检索）与 ES（关键词检索 + 术语聚合），LLM 批量实体抽取结果随 chunk 入 ES（详见 6.5）
- **审核回流 approve 走本流水线（闭环生效唯一机制）**：approve 时写映射台账（dept_mapping，source=feedback）+ 由审核结论**生成合成 chunk**（症状→正确科室的鉴别语料，如"颈肩僵硬伴手指放射性麻木建议首诊骨科，可与心源性胸痛鉴别"；文本由 LLM 生成、管理员预览定稿，见链路 C「审核产出规则」），挂系统级「回流知识文档」容器，走本流水线完整入库（切分→向量化→pgvector+ES 双写→术语抽取→白名单刷新）。RAG 检索到合成 chunk 后，同类主诉推荐变对——准确率曲线抬升的全部来源。dept_mapping 运行时不被 RAG 消费，仅作管理端台账（可查"哪些症状补过映射、谁 approve 的"）

### 链路 C：反馈闭环链路（差异化核心）

```
患者端三阶段                         后端（比对+聚合+审核）         管理端
───────┬───────────────────────────┬───────────────────────────┬──────
  ① 推荐卡片渲染 ── result_view ──→ │ 埋点落库                    │
  ② 模拟挂号页选科室 ─ sim_register→│ 埋点落库                    │
  ③ 挂号成功确认 ── register_success│ 埋点落库                    │
       │            (deptId)      →│ ④ actual_dept 写入导诊记录  │
       │                            │   （挂号确认接口同事务写入   │
       │                            │    guide_record，非 feedback）│
       │                            │ ⑤ 比对 top1_hit/top3_hit   │
       │                            │ ⑥ 定时聚合(每小时):         │
       │                            │    错误样本按方向聚类        │
       │                            │    分类型阈值升级待审核      │
       │                            │ ⑦ 看板实时反映准确率        │
       │                            │                          │ ◄── ⑧ 审核队列
       │                            │                          │ ⑨ approve(主科室+交叉科室)
       │                            │ ◄─────────────────────── │
       │                            │ ⑩ 写映射台账+合成chunk走链路B │
       │                            │ ⑪ 记录变更日志            │
       │                            │ ⑫ 看板准确率变化（闭环生效）│
```

**对齐点**：
- **模拟挂号 = 产品引导、非数据保证**：出结果后引导患者完成挂号确认，但患者可以不挂（流失）——会话保持 ongoing、actual_dept 为空；准确率口径 = **已确认记录**（actual_dept 非空）的命中率，不做 100% 完整性假设
- **新主诉判定（双信号）**：会话**已出 result**（有导诊记录）或已 closed 后，患者的下一条输入 = 开新会话、新主诉；未出 result 的 ongoing 会话内，输入按追问/补信息处理。挂号成功（register_success）仍置 closed——两种路径殊途同归，判定信号都是状态机字段，不做模型语义判断
- 三个埋点阶段的页面归属：推荐卡片页 / 模拟挂号页 / 挂号成功页——缺一不可，前端流程必须串通
- `actual_dept` 只认 register_success 触点的 deptId，且由**挂号确认接口同步写入** guide_record（与 top1_hit/top3_hit 同事务）；未挂号记录 actual_dept 为空、不参与准确率计算；前面阶段（result_view / sim_register）与 track_event 全部触点只作时间过程信号（漏斗/耗时分析），非准确率事实来源
- track_event 埋点保留旁路落库，仅服务看板漏斗/耗时/交叉分析，不参与准确率计算
- **主从关系**：准确率唯一依据 = actual_dept（行为比意见可信）；**不做卡片 👍/👎 主观评价**（埋点三触点的漏斗/耗时分析已足够覆盖辅助分析，主观评价不参与任何统计）
- 聚合口径：仅统计"已反馈且未命中 top3"的记录（真正的系统错误）；**top-1 未命中但 top-3 命中视为"排序精度问题"，不进回流**（患者本来就在推荐列表里选的，不算系统错）
- 分类型升级阈值：top-3 未命中（错误样本信号）升级阈值低（默认 3 次）；已审核模式的新样本自动归类，不重复升级
- 闭环生效的可观测信号：**准确率趋势曲线在审核确认后出现抬升**（答辩/演示的关键证据）

#### 错误模式聚合算法（两级归桶，定时任务每小时）

**设计哲学**：聚合是召回机制（宁可混、不能漏——漏了问题永远不升级，混了只是多审一条）；人工评审是质量闸门——聚合只负责把候选升级到管理员面前，**写回知识库的唯一路径是人工 approve**。

```
输入：已挂号反馈 且 top3_hit=0 的新记录（aggregated=0 增量标记，与聚合同事务置位防重复计数）

第一级 · 精确归桶（规则，零成本）：
  症状归一化（去口语词/标点/量词，固定词典）→ 与【同方向】桶的精确键比对 → 命中即归入
第二级 · 语义归桶（向量，处理同义不同形）：
  精确未命中 → 症状文本 embedding 向量化 → pgvector 中在【同方向】桶的锚点向量里
  找余弦相似度最高者（cluster_bucket_vec，direction 作为 SQL 过滤条件）
  → 相似度 ≥ 阈值（默认 0.85，可配）归入该桶；否则开新桶，本样本作锚点（锚点固定不漂移，
  开新桶时向 pgvector 写入锚点向量，归 feedback 模块直写）

升级：count ≥ 阈值（top3 未命中默认 3 次）且 status=monitoring → pending 进待审队列
```

**关键规则**：

- **方向预筛**：语义匹配只在（推荐科室, 实际科室）相同的候选桶内进行——方向是精确字段先锁死，歧义空间小
- **分类型口径**：top-3 未命中（错误样本信号）才进聚合；top-1 未命中但 top-3 命中 = 精度问题，不进回流
- **幂等**：已终态（approved/rejected/dismissed）桶的新样本只计数、绝不**自动**重新升级；管理员可对终态桶发起「修正重审」回 pending（人工纠错，不受幂等约束，见「审核产出规则」）
- **低置信度分流**：推荐 top1 置信度 < 0.5 → 不进聚合，进盲区榜
- **不调 LLM**：聚合是批量确定性活，规则 + 向量足够；语义歧义（同病不同表述、一词多义）交给人工审核兜底——审核看代表样本原文，不看归一化串

#### 审核产出规则（approve 的完整机制）

**审核动作（管理员在审核页对 pending 桶操作）**：

- 给出**主科室**（必需）+ **交叉科室**（可增删）。交叉科室 = 该症状还需一并考虑、需鉴别的其他科室——回答"还可能是什么"，不是"系统当时误判成了什么"
- 交叉科室**预填 = 桶方向 {推荐科室, 实际科室} − {主科室}**（集合差一条规则，无特例）；预填只是建议值，管理员可增删
- 科室选择器**包含停用科室**（带"已停用"标记）——停用仅入口生效，审核是知识层动作、与运营启停解耦
- 合成 chunk 文本由 **LLM 生成**（经 llm 适配层，输入 = 症状原文 + 主科室 + 交叉科室）→ **管理员预览可编辑定稿** → 确认（LLM 失败用模板兜底）——守住"写知识库唯一路径是人工 approve"的完整闸门

**确认产出（一个动作，两样产物）**：

- 映射台账：键 = **(症状原文, 主科室)**——同键合并（交叉科室取并集）、异键开新行；source=feedback。同一症状可因不同方向的桶被审出多个主科室，各占一行
- 合成 chunk：挂系统级「回流知识文档」容器，走链路 B 流水线（见链路 B 对齐点）
- 落库边界：台账 + 桶状态**同事务**；chunk 入库**异步**，失败随合成文档在知识库页可见、可 reprocess（沿用链路 B 任务表机制）

**否决（不产出）**：

- **驳回（rejected）**：审过否定——人看过并判定"不是系统错"（如患者挂错、样本误聚合）。逻辑：回流只修错，没错可修就关桶
- **忽略（dismissed）**：没审就关——测试/演示数据、重复桶等噪声的清理动作

**纠错（人工闸门有权改自己）**：

- 终态桶（approved/rejected/dismissed）可发起**修正重审**：回 pending 重走审核——重新生成预览、按键规则处理台账旧行（旧行删除/新行写入）、替换合成 chunk
- 边界：幂等规则只约束**自动**升级（防聚合反复翻烧饼）；人工重开是显式动作，不受限

#### 证据快照与根因归因（链路 C 的配套诊断能力）

**为什么需要**：全错（top-3 未命中）的根因不一定是"知识库缺内容"。知识库规模大时，主要矛盾在**离线入库/检索层**（切分破碎、解析遗漏、检索失败）和**回答层**（模型没用检索内容、结构化失败）。只靠"补知识"治标不治本，必须先归因、再对症修复。

**证据快照**：每条导诊记录留存 RAG 证据快照（JSON），让审核时能还原"系统当时看到了什么、怎么答的"：

```json
{
  "retrieved": [{"chunk_id": "12", "title": "骨科科室介绍", "score": 0.71, "rank": 1}],
  "retrieved_query": "（查询改写后的实际检索串；未改写时等于患者原话）",
  "model_cited": [1, 2],
  "prompt_snippet": "（实际送入模型的上下文摘要）",
  "model_output_raw": "（模型原始输出）"
}
```

> `score` 是精排（rerank）相关度；精排降级（退化为 RRF 顺序）时该值为 RRF 分（量级 ~0.01），日志有 WARN 可查。

**归因交互（管理员审核页）**：展示完整证据链后，管理员用**预定义选项多选**标注根因（不做自由文本填空，保证统计口径统一）。归因以**桶级一键套用**（写入桶内每条记录，存储仍是 per-record）为主，展开后可**逐条覆盖**；归因与 approve/驳回是同一页上的**两个独立动作，互不代劳**（驳回桶不改归因，标归因不改桶状态）：

| 存 key | 显示名 | 含义 | 修复方向 |
|--------|--------|------|---------|
| `chunk_broken` | 切分破碎 | 语义被拦腰切断、标题与正文分离 | 对该文档局部重入库（调整切分参数） |
| `parse_missed` | 解析遗漏 | docx 表格/格式内容被丢弃 | 修复解析器后重入库 |
| `retrieval_fail` | 检索失败 | 内容在库但未召回（表述不匹配/Top-K 不足） | 调检索参数（混合检索、扩大 Top-K） |
| `mapping_missing` | 局部映射缺失 | 交叉症状映射没有覆盖 | 补映射（回流） |
| `model_ignored_retrieval` | 模型未依据检索 | 检索到了但模型凭常识答 | Prompt 版本迭代 |
| `structure_fail` | 结构化失败 | JSON 解析异常、幻觉科室 | 输出校验与重试机制 |
| `patient_wrong` | 患者挂错 | 非系统责任 | 标根因（看板统计信号）+ 驳回桶（审过否定）——两个动作都要做；看板 miss 口径排除已标此项的记录 |

**归因可更新**：归因结果允许事后修改（单选变更多选、错误标注纠正），每次修改记录操作人与时间，并追加写入 `root_cause_log`（修改历史留痕，不覆盖，纯审计不参与聚合）；看板"**错误根因分布**"基于 `root_cause` 最新归因实时聚合——分布图直接指示下一步动作（检索层占比高 → 调参；回答层占比高 → 修 Prompt）。

**存 key、显示 label（实现定案）**：`causes` 存上表左列的小写 key，页面显示中列的中文名——文案可改、口径不动。字典唯一定义源是后端 `feedback/enums/RootCauseKey`（key 由常量名派生，防漂移）；后端不校验存量值，字典外的 key 原样显示并记 WARN（统计可以少一行解释，不能把数据藏起来）。`patient_wrong` 是唯一带后端语义的一项：看板准确率口径靠它排除记录。

### 链路 D：鉴权链路

```
患者/管理员 → 登录(POST /api/auth/login) → JWT 签发(含角色)
    → 前端存储(Axios 拦截器携带 Authorization)
    → JwtAuthFilter 校验 → SecurityContext 注入
    → 接口鉴权：/api/admin/** 仅 ROLE_ADMIN；/api/chat/**、/api/guide/** 仅登录
```

**对齐点**：`/api/auth/register` 默认患者角色，注册成功即登录（后端直接签发 token）；管理员账号由初始化脚本/管理端人工创建；未登录/登录过期返回 401（前端清登录态跳登录），无权限访问受限接口返回 403（不跳转，仅提示）；前端 HTTP 层统一解包 Result——业务码 2000 同样按未登录兜底跳登录，`code!==200` 统一 reject 携带 message 供页面展示。

**登录时只校验封禁，不看禁言**：`status=banned` → 拒绝登录；`mute_until` 未到期仍可登录（能看历史、能重新描述症状被驳回），限制只作用在"发言"这一步（入口校验先判禁言，见 6.6）。两者混用会让"禁言"变成"封号"，且封禁语义被稀释（管理端分不清谁封的）。

---

## 3. 模块职责边界与依赖关系

```
        ┌────────────┐      ┌────────────────┐
        │ 用户/鉴权   │      │  问诊对话编排    │──────┐
        └────────────┘      └────────────────┘      │
                                  │                  ▼
                                  │           ┌────────────┐     ┌──────────────┐
                                  │           │ RAG 检索层  │────▶│ LLM 适配层   │──▶ 外部 API
                                  │           │ 改写/召回/  │     │ DeepSeek/阿里│
                                  ▼           │ 重排/Prompt │     └──────────────┘
        ┌────────────┐      ┌────────────────┐└────────────┘
        │ 反馈闭环    │◀────▶│ 知识库管理      │──────┐
        │ 聚合/审核/  │ 回流 │ 文档/映射维护   │      │
        │ 回流        │      └────────────────┘      │
        └────────────┘          │ 离线侧             │
              │                 │ (线程池+任务表)      │
              ▼                 ▼                    ▼
        ┌────────────┐      ┌────────────┐      ┌──────────────┐
        │ MySQL      │      │ MinIO      │      │ pgvector     │
        │            │      │ 文档对象存储│      │ kb_chunk_vec │
        │            │      └────────────┘      │              │
        └────────────┘                          └──────────────┘
```

**硬性规则**：
- 对话编排 → RAG → LLM，单向调用，不反向；**chat 不直连 LLM**，流式生成经 `RagService.streamAnswer`（rag 是 chat 侧唯一的生成入口，chat 只依赖 rag 的消息类型 `RagTurn`）
- 知识库管理 = 唯一写向量库的入口（上传流水线 + 回流同步）
- 反馈闭环只读导诊记录、只写映射与知识片段，**不写对话与导诊记录本身**
- rag 需要读切片正文时，用**端口 + 实现**解耦：rag 声明 `ChunkTextProvider`，kb 实现（kb → rag 单向依赖），rag 因而不依赖任何业务模块
- 敏感词命中留痕同理：chat 只发 `SensitiveHitEvent`，feedback 监听落 `filter_log`（feedback → chat 单向依赖，不反向）；**窗口计数的读取也按这个方向**——chat 声明端口 `SensitiveHitCounter`、feedback 实现（与 `ChunkTextProvider` 同一模式），chat 拿得到计数但不依赖 feedback
- 旁路组件（Spring 事件）不参与主流程事务：留痕/埋点失败只记日志，绝不阻断导诊
- 统计看板（stats）是**纯读侧**：只读 `guide_record`/`chat_message`（chat）、`root_cause`/`review_task`（feedback）、`dept`（kb）做聚合，不写任何业务表；聚合归 stats、接口归 admin

---

## 4. 部署形态（开发/演示期）

- 单后端工程（Spring Boot），本机运行；pgvector 单 Docker 容器
- 前端单工程（Vite dev server），`/api` 代理到后端
- 预置演示数据：5+个科室蓝本知识库（手工构建语料），管理端一个管理员账号
- **种子语料（开发夹具）**：`guide.seed.enabled=true` 启动时装载 7 科室 12 条鉴别语料 + 72 条术语白名单，
  走 kb 唯一写入口直写 MySQL + pgvector + ES（与上传流水线共用同一套写入代码，差的是解析/切分与异步编排）；
  科室表非空即跳过，幂等。链路 B 上传流水线就绪后关闭本开关

---

## 5. 已定参数汇总

| 参数 | 值 | 说明 |
|------|-----|------|
| （本表为默认值） | — | 标注"可配"的参数运行时从 MySQL `sys_config` 字典表读取，管理端可调；默认值即本表 |
| 追问轮数上限 | 3 轮 | 超限不强求，出低置信度结论接盲区榜分流；sys_config 键 `chat.ask.max.rounds` |
| 科室蓝本规模 | 5+ 科室 | 覆盖常见症状，影响演示检索效果 |
| 模拟挂号科室范围 | 全部启用科室 + 推荐高亮 | 必选模式唯一入口 |
| 聚合升级阈值 | top-3 未命中 3 次 | 分类型阈值；演示可临时调低 |
| 检索 Top-K / 重排 Top-N | 见 sys_config（初始 10 / 5） | RAG 链路参数，键 `retrieve.top.k` / `retrieve.top.n`；pgvector 与 ES 各召回 Top-K，融合后精排取 Top-N。代码里的兜底默认是 20 / 5，运行时以 sys_config 为准 |
| RRF 融合 | 倒数排名求和，k=60 | 混合检索融合算法，纯代码实现 |
| 语义归桶相似度阈值 | 0.85 | 可配 |
| 低置信度分流感阈值 | 0.5 | 推荐 top1 置信度低于此值进盲区榜（low_confidence=1） |
| 敏感词统计窗口 | 60 分钟 | 滑动窗口，键 `sensitive.window.minutes` |
| 禁止词警告 / 禁言阈值 | 10 / 30 次（词次） | 键 `sensitive.banned.warn.count` / `sensitive.banned.mute.count`；禁言阈值须大于警告阈值 |
| 观察词警告阈值 | 25 次（词次） | 键 `sensitive.watch.warn.count`；观察词**不禁言** |
| 禁言时长 | 60 分钟 | 键 `sensitive.mute.minutes`；到期自动解除 |
| 追问轮判定 | 规则硬门槛 + LLM 合并判定 | 详见链路 A 配套 |

管理端可调范围是**白名单制**：链路参数 7 个键（§7.1）+ 敏感词处置 5 个键（§6.6）可在线改（改完立即生效）；模型名与 API Key 不走 `sys_config`，改配置重启。

---

## 6. 敏感词过滤（链路 A 配套，chat 入口前置）

**场景定位**：医院导诊的敏感词与通用内容审核不同——部位名词（胸、腹、下体等）是分诊核心信息，**不能拦**；真正要拦的是过激言论、脏话、辱骂。"疼死了""要死了"是真实痛苦表达，不拦。

### 6.1 校验顺序（chat 链路对话入口前置，先于信息充足性判定）

```
患者消息进来
  ① 命中禁止词（banned），但该词本身命中医学术语白名单 → 放行，正常分诊（防误杀，白名单优先）
  ② 命中禁止词（banned）→ 拦截：返回固定引导"请描述您的症状，我来帮您分诊"，
     不调模型、不进导诊流程；过滤事件旁路落库（filter_log + 词库命中次数累加）
  ③ 命中观察词（watch）→ 放行 + 记录打 watch 标记，正常走导诊
  ④ 均未命中 → 放行，正常分诊
```

> **判定口径（实现定案）**：白名单的"优先"作用在**命中的那个敏感词**上——词库里混入部位/症状词（如"下体"）时不拦；但主诉里出现医学术语不代表整句免疫，句中另有非术语的禁止词（辱骂、过激言论）照样拦截。这样"宁可少拦、不能误杀"与"真拦违规内容"两个目标同时成立。
> 词库**实时查表**（表小且带索引），不缓存、不落 Redis——管理端增删停用即时生效。

### 6.2 三层设计

- **第一层 · 敏感词库（词典精确匹配，零成本）**：分禁止词/观察词两类，管理端可单条添加、批量导入（txt 一行一词，自动去重）、导出备份
- **第二层 · 医学术语白名单（防误杀，优先级高于敏感词）**：由 LLM 对切分后 chunk 批量提取实体（部位/症状词），随 chunk 存入 ES，**ES terms 聚合得到全库术语清单（候选池）→ 人工确认（或关闭人工审核开关直接生效）后落 MySQL `medical_term` 表 = 唯一生效源**；chat 入口校验从该表加载内存 Set；知识库更新（approve 回流/重新入库）时同步刷新。详见 6.5
- **第三层 · 不引入 NLP 审核模型做拦截判定**：过滤判定是确定性活，词典 + 白名单足够；术语提取用 LLM（离线批量），拦截动作不调 LLM

### 6.3 落点

- 链路：chat 模块入口前置；词库校验实时查表（或本地缓存 + 更新失效，不存 Redis）
- 埋点：拦截/观察事件旁路落库，不阻塞主流程
- 管理端：用户管理页"敏感词库管理"tab（词/类型/命中次数/状态；观察词可一键转禁止词）
- 核心取舍：**宁可少拦（词典保守），不能误杀（白名单优先）**——误杀一次正常主诉，患者即流失

### 6.4 管理端新增用户管理职责

- 用户封禁与解封（auth 链路：登录时校验账号状态）
- 敏感词库管理（本节）
- **违规处置的观测与配置**：用户行显示窗口内触发词次与当前处置（警告次数 / 禁言至）、可看违规明细、可"立即解除禁言"；处置规则（窗口与阈值）与词库同页维护（6.6）

### 6.5 医学术语白名单的提取机制（LLM 批量实体抽取 + ES 聚合）

```
入库流水线（链路 B）切分完成后
  ① LLM 批量实体抽取（走 llm 适配层，离线）：一次传多个 chunk（批量合并，摊薄成本），
     prompt 要求输出 chunk 涉及的部位/症状术语
  ② chunk 存 ES（kb 唯一写入口）：chunk 全文 + 提取出的 medical_terms 字段
  ③ ES terms 聚合 medical_terms → 全库术语清单（候选池）→ 人工确认/自动生效后落 MySQL medical_term = 白名单
  ④ 管理端查看：知识库管理页术语清单面板（只读 + 人工可增删/停用）
  ⑤ 知识库更新（回流/重新入库）→ 新 chunk 抽取入 ES → 白名单自动刷新；
     文档删除 → 该 chunk 的术语随 chunk 删除，聚合结果自动收敛
```

- 白名单使用：chat 入口前置校验（6.1 顺序），内存 Set 一律从 MySQL `medical_term` 表加载（首次访问惰性构建 + 60s TTL，另有显式 `refresh()` 供管理端写入后立即失效）；ES 聚合只是候选池——LLM 抽取新术语落库默认 enabled=0，受"人工审核"开关控制（开启时需管理端确认启用，关闭时直接启用）；管理端增删/停用最长一分钟内影响线上校验
- 术语清单同时服务于管理端"看得到系统认识哪些词"，是知识库覆盖度的直接观测
- 双写一致性：chunk 双写 pgvector + ES 在同一事务边界，删除补偿顺序为 先删 ES → 再删向量 → 再删元数据 → 再删 MinIO

### 6.6 按用户的累计处置（警告 / 禁言）

命中留痕（6.3）回答"系统看见过什么"；本节回答"同一个用户反复触发之后怎么办"——**滑动窗口内累计命中词次**达阈值即处置。

**阈值（`sys_config`，管理端「敏感词库 → 处置规则」可调）**

| 键 | 默认 | 含义 |
|----|------|------|
| `sensitive.window.minutes` | 60 | 统计窗口（分钟，滑动） |
| `sensitive.banned.warn.count` | 10 | 窗口内禁止词命中词次达此值 → 警告 |
| `sensitive.banned.mute.count` | 30 | 同上前提下达此值 → 禁言 |
| `sensitive.watch.warn.count` | 25 | 窗口内观察词命中词次达此值 → 警告（**不禁言**） |
| `sensitive.mute.minutes` | 60 | 禁言时长（分钟，到期自动解除） |

**三条口径**

- **按词次而非消息数**：一条消息里命中两个观察词算两次。实现上入口校验**收集全部命中**再统一落库，不能"遇到第一个禁止词就返回"——那样累计次数会明显偏低，阈值永远够不到
- **滑动窗口**：只看最近 N 分钟，不做自然小时对齐；窗口与阈值在同一次判定里**只读一次**（规则快照），避免中途被管理端改参数而产生两套口径（按 60 分钟统计却按 30 分钟记留痕）
- **同一窗口内同类处置只做一次**（警告与禁言各一次）：判据是 `user_violation` 里窗口内已有同类记录。警告判重是为了不念叨（一过阈值每轮都被提醒）；禁言判重是为了不"续禁"——阈值看的是窗口内累计次数，而次数不会因为一次禁言清零，不判重的话管理员「立即解除禁言」后，用户下一条消息就会被旧计数重新禁上，解除形同虚设

**处置与生效时机**

| 触发 | 动作 | 是否阻断本轮 |
|------|------|------------|
| 禁止词达警告线 | 写 `user_violation(level=warn)` + 对话内 `notice` 提示 | **本轮仍拦下**（命中禁止词本就拦截，见 6.1）；`notice` 是附加提示，不等于放行 |
| 禁止词达禁言线 | 写 `user.mute_until` + `user_violation(level=mute)` | **本轮即拦下**，此后每次发言先判禁言 |
| 观察词达警告线 | 写 `user_violation(level=warn)` + `notice` 提示 | 不阻断；观察词**永不**禁言 |
| 禁言期内发言 | 返回禁言话术（含剩余分钟） | 拦下，且**不匹配词库、不计违规** |

判定优先级：禁言 > 警告（跨过禁言线就不再补一条警告，用户已经在被处置）；禁止词 > 观察词（一次只处置一次，按更重的一侧留痕）。

**禁言 ≠ 封禁**

- 封禁（`user.status=banned`）是**管理员**的处置：登录即拒绝、需人工解封
- 禁言（`user.mute_until`）是**词库累计触发**的处置：**只禁发言、仍可登录**（患者能查看历史），到期自动解除；管理端可"立即解除"
- 到期**不需要定时任务**：判定就是"当前时间是否超过 `mute_until`"。少一个必须准时运行的组件——定时任务停摆会让禁言永久生效，而这种故障很难被发现
- 禁言期内不累计违规出于同一考虑：否则用户随便发几句就自我续禁，禁言期滚动延长、永远出不来

**留痕记的是口径快照**：`user_violation` 存当时窗口内命中词次、窗口长度、阈值、触发类型。阈值可调，事后追溯一条历史处置必须能复现当时的判定依据。

**计数从哪来（依赖方向）**：计数读 `filter_log`，而它是 feedback 的表，依赖方向是 feedback → chat 单向。chat 声明读端口 `chat/port/SensitiveHitCounter`、feedback 实现，与 rag 的 `ChunkTextProvider` 同一套路——chat 拿到窗口计数却不依赖任何业务模块。

**话术**：警告与禁言话术走 `prompts.yml`（`chat.warn-reply` / `chat.mute-reply`，后者必含 `{minutes}`，缺失会被启动校验拦住），与拦截话术同一套机制；出口是独立的 `notice` SSE 事件（见链路 A 对齐点）。

---

## 7. 管理端控制台（运行时配置面）

管理端页面壳与视觉（冷靛控制台 方案 B）已定稿。本节记的是管理端的**设计口径与边界**：数据从哪来、哪些能改、改完何时生效、越不过哪条线——接口明细（路径/字段）按文档定位仍搁置，以各节「落点」里点到的 controller / service 为准。

### 7.1 LLM 配置页

**模型接入信息（只读）**：
- 模型名 / base-url / embedding 维度来自 `application.yml` 的 `llm.*`；**API Key 来自 `application-local.yml`，不进库、不出后端**——接口只回"是否已配置"的布尔位，**不回值也不回掩码串**
- 在线改模型或密钥这条路刻意不做：一旦允许写库，密钥就进了数据库备份与审计日志；改模型改配置重启

**运行时参数（可改，白名单制）**：
- 受管键、类型、取值范围、默认值、说明全部写在代码侧（`SysConfigAdminService` 的 `ParamSpec`），页面只能改这些键的**值**——不让前端决定能写哪些配置，否则字典表会被塞进任意键，或以"页面上有输入框但没有任何代码读它"的形式产生假配置
- 受管键就是管理端能改的**全部范围**，7 项与 §5 已定参数一一对应：`retrieve.top.k` / `retrieve.top.n` / `chat.ask.max.rounds` / `guide.upgrade.rounds` / `cluster.bucket.threshold` / `guide.low.confidence` / `term.manual.review`（后两项服务于链路 C 后半的聚合与升级判定）
- 后端按类型二次校验（int 范围、小数 0–1、布尔只认 true/false），前端控件只是提示，**不信任前端**；不在白名单的键直接拒绝（错误码 2008）

**保存语义（实现定案）**：
- **先全量校验、再统一写入**——"有一项非法则一项都不落库"是校验阶段的自然结果，不依赖事务回滚
- 写完调 `SysConfigService.refresh()` 让进程内缓存**立即**失效，不等 60s TTL；清缓存必须发生在所有写操作**之后**，否则别的线程会在提交前的窗口期用旧值把缓存重新填上
- 值缺失时回落到代码侧默认值（首次部署无需先灌 sys_config 就能开页面）

**连通性探测（人工触发，同步返回）**：
- 三路各发一次最小请求：对话一句问候 / 一条文本向量化 / 一对候选精排；**探测不走 SSE 线程池**，避免占用在线导诊线程
- 每路独立兜异常，一路失败不影响其余两路结论；向量维度与配置不一致单独判失败（提示换模型需重建向量）
- 失败原因**先按配置里的密钥做替换再截断**：上游响应体可能回带 Authorization 头，直出等于把 Key 送到管理端页面上；截断防止一串上游响应体糊满页面

**落点**：`admin/controller/LlmConfigAdminController`（`/api/admin/llm/{models,params,probe}`，仅 ROLE_ADMIN）→ `auth/service/SysConfigAdminService`（参数读写）/ `llm/service/LlmDiagnosticService`（模型信息与探测）

### 7.2 数据看板页

**准确率口径（后端统一算完再下发，前端只负责显示）**：
- 分母 = 已确认挂号（`actual_dept_id` 非空）**且**非低置信度 **且**未被标注「患者挂错」的导诊记录
- 三条排除各有出处：未挂号没有事实可依（模拟挂号是产品引导、非数据保证，不做 100% 完整性假设）；低置信度进盲区榜、不参与准确率统计；「患者挂错」是归因表里明确的"非系统责任"（见链路 C 根因表）
- 分子 = `top1_hit = 1`（Top-1 命中率）/ `top3_hit = 1`（Top-3 命中率）
- **样本为 0 时显示「—」而不是 0%**：0% 会被读成"全错"，与"还没有数据"是完全不同的两件事
- 环比 = 近 N 天 vs 前 N 天，给的是**百分点差（pp）**；上期无样本时明说"不可比"，不伪造成一个数字

**每日趋势（命中构成）**：
- 与 KPI 同一口径（已反馈集合），按 `DATE(created_at)` 分组：命中 = `top3_hit=1`，未命中 = `top3_hit=0`——Top-1 与 Top-3 之间的差（排序精度）由 KPI 卡体现，两段堆叠图不表达第三态
- 没有数据的日期**补 0**，不能让图错位；柱高按窗口内最大值归一化并留 20% 余量（真实日量可能远超 100 条，直接当百分比会溢出容器）

**错误根因分布**：
- 数据源 = `root_cause` 的**最新归因**（可事后修改；`root_cause_log` 只做审计、不参与聚合）；一条记录可标多个根因 → 按**标注项**计数、降序，百分比 = 该项数 / 全部标注项数
- 库里存**小写 key**、下发**中文名**：字典唯一来源是 `feedback/enums/RootCauseKey`（见链路 C 的选项表）；后端不校验存量值，字典外的 key 原样显示并记 WARN
- `patient_wrong` 是唯一带后端语义的一项——它决定准确率口径排除哪些记录；判定用 LIKE 粗筛后**再按数组元素精确比对**（避免 `patient_wrong_pending` 这类新 key 被子串误伤）
- 表为空时页面显示空态并指向审核页（根因由审核页标注），不是显示 0 分布

**最近导诊记录**：
- 主诉摘要 = 该会话的**第一条患者消息**（按页批量查一次，不在循环里回表）；截断 40 字
- 科室名用 `dept` 表直查而不是 `DeptService.listEnabled()`：停用科室的历史记录也要显示得出科室名
- 结果列四态：未反馈（`actual_dept` 为空）/ top-1 命中 / top-3 命中 / **未命中**——top-1 未中但 top-3 中属于**排序精度问题、不是系统错**（与链路 C 的聚合口径一致），不显示成"未命中"
- 置信度为 null（模型未给合法值或兜底科室）显示「—」，不显示 0%

**落点**：`admin/controller/DashboardAdminController`（`/api/admin/dashboard/{overview,records}`，仅 ROLE_ADMIN）→ `stats/service/DashboardService`（纯读聚合，不写业务表）

### 7.3 管理端边界与约束

- **管理端是运行时配置与审阅面，不参与在线导诊主流程**：不占 SSE 线程池（连通性探测这类同步外呼同理）、不写 `chat_message` / `guide_record`。导诊记录只由链路 A 写，`actual_dept` 只由挂号确认接口写
- **分层**：页面 → `/api/admin/**` controller（admin 是启动器，只做编排）→ 业务模块 service。参数归 auth（`sys_config` 的属主）、模型信息与探测归 llm（适配层自己的知识）、聚合归 stats（纯读侧）、审核与回流归 feedback
- **口径与参数一律后端定，前端只显示**：能改哪些键（白名单）、取值范围、准确率分母、环比是否可比，全部在服务端判定；前端控件只是提示，不构成约束
- **approve 的落库边界（链路 C）**：映射台账与桶状态**同事务**；合成 chunk **必须走链路 B 流水线**入库——`kb` 是唯一写向量库与 ES 的入口，**不得为审核回流另开直写通道**
- **只读观测类面板归属管理端职责**：`filter_log`（敏感词命中留痕）与 `track_event`（三触点埋点）是管理端应当能查的过程信号；它们只作漏斗/耗时参考，不参与准确率统计（准确率事实仍只有 `actual_dept`）
